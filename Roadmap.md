# Pokkum Roadmap

See [Vocabulary.md](Vocabulary.md) for the full CLI flag reference, naming
conventions, and the rationale behind each new flag noted below.

See [fixes-to-v1.md](fixes-to-v1.md) for a post-v1.0 audit that found
several `[x]` items below overstated what they actually did, and the fixes
applied for each; [for-users.md](for-users.md) for what changed as a
result.

## v0.1 (Completed)

- [x] Reproducible layer timestamps: set every layer/config timestamp to `SOURCE_DATE_EPOCH` derived from the last git commit (`git log -1 --pretty=%ct`), not build time
- [x] Minimal `securityContext` in generated manifests: Default to `runAsNonRoot: true`, `allowPrivilegeEscalation: false`, `capabilities.drop: [ALL]`, `seccompProfile: RuntimeDefault`.
- [x] `.pokkumignore`: Explicit exclude list so `.env.local`, test fixtures, and source maps don't end up embedded
- [x] Dry-run mode (`--dry-run` / `--print-manifest`): show what would be built/pushed/applied without doing it - added to `pipeline.go`
- [x] Structured, leveled logging so CI logs are parseable
- [x] Idempotent registry pushes: skip push if the computed digest already exists remotely (`remote.Head` before `remote.Write`)
- [x] Basic config validation surfaced as clear CLI errors before any network call — fail fast, not mid-push

## v0.2 (Completed)

- [x] Cosign signing
- [x] SLSA provenance attestation
- [x] SBOM as OCI 1.1 referrer instead of `.sbom` tag convention (new flag: `--sbom-attach=referrer|tag`, default `referrer`)
- [x] `pokkum dev` — Hot-Reload Container Development (build + `--local` + run in one command; new flag: `--debug` to drop into a shell inside the container)
- [x] Zero-Config Auto-Injection: Auto-injecting the adapter and `SOURCE_DATE_EPOCH` pinning without manual `svelte.config.js` edits (supported via `--inject` and `--no-inject` flags). (see [pokkum-injection-concept.md](concepts/archive/pokkum-injection-concept.md)) — **correction (2026-08-17):** as implemented, this had no effect on real builds; see "Layered-Strategy Real-Build Correctness" below for the finding and [concepts/zero-config-injection-concept.md](concepts/zero-config-injection-concept.md) for the fix actually shipped (a fast, actionable error on misconfiguration, not a self-correcting build).
- [x] Base Image Lockfile (`pokkum.lock`): Enable reproducible base image resolving (distroless/chainguard) to prevent drift. (new flags: `--update-base`, `--offline`; new subcommand `pokkum base update --preset <name>`) (see [pokkum-lock-concept.md](concepts/archive/pokkum-lock-concept.md))

## v0.3: Layer Caching & Core Architecture Shift

_Refactoring to replace the `exe` adapter with a hand-rolled adapter for layer caching, dramatically reducing per-commit image size._ (see [pokkum-layer-caching-concept.md](concepts/archive/pokkum-layer-caching-concept.md))

- [x] M1: Packager + runtime plumbing (Bun runtime resolution, pinned downloads). (new flags: `--bun-binary=<path>` offline escape hatch, `--bun-variant=baseline` for pre-AVX2 hosts) (see [pokkum-layer-caching-concept.md](concepts/archive/pokkum-layer-caching-concept.md))
- [x] M2: Hand-rolled SvelteKit adapter + Phase-1 layering (separating app JS from runtime). (new flag: `--strategy=layered|exe`, `layered` default) (see [pokkum-layer-caching-concept.md](concepts/archive/pokkum-layer-caching-concept.md))
- [x] M3: Vendor splitting (`bun build --splitting`) + native closure support (unblocking native `.node` addons). (no new flag — internal to `--strategy=layered`) (see [pokkum-layer-caching-concept.md](concepts/archive/pokkum-layer-caching-concept.md))
- [x] M4: Hardening (`readOnlyRootFilesystem`) & cutover from the old `exe` adapter. (no new flag — folds into `--security-context`'s hardened-defaults bundle) (see [pokkum-layer-caching-concept.md](concepts/archive/pokkum-layer-caching-concept.md))
- [x] Image Optimization: Deduplication across layers, optional zstd layer compression. (new flag: `--compression=gzip|zstd`, default `gzip`) (see [pokkum-layer-caching-concept.md](concepts/archive/pokkum-layer-caching-concept.md))

## v0.4: Unified Telemetry & Developer Experience

- [x] Unified Metrics & Telemetry: Zero-config injection of OpenTelemetry setup for traces and metrics (`pokkum metrics` and app-side). (new flag: `--metrics-port` on the `pokkum metrics` subcommand; build-time half reuses existing `--telemetry`/`--otel-export` family) (see [pokkum-metrics-otel-concept.md](concepts/archive/pokkum-metrics-otel-concept.md))
- [x] `pokkum init` & Project Configuration (`.pokkum.yaml`): Interactive setup wizard, rich configuration schema (profiles like `local`/`production`, OCI image metadata/ports/env, security fail-on-cve gates, SBOM, cache, OTel), `pokkum build --profile <name>`, and config inspection tools (`pokkum config view`, `pokkum config validate`).
- [x] `pokkum doctor`: Environment preflight checks (Bun version, registry auth, `svelte.config.js` sanity). (new flag: `--fix` for mechanical repairs)
- [x] Interactive Failure Diagnostics: Automatic log dump and exit code analysis on local container failure. (new flag: `--no-diagnostics` opt-out for CI)
- [x] `--output=json`: Machine-readable build results schema for robust CI parsing.
- [x] Diff & Explain: `pokkum diff`, `pokkum explain`, and `pokkum why` to trace layer changes and dependencies. (reuses `--output=json` for machine-readable output; no new flag)

## v0.5: Reproducibility & Diagnosis

_Closing the loop on reproducibility with verifiable rebuilds and non-determinism bisection._

- [x] M0: Provenance completeness (recording Go version, builder OS/arch, lockfile hashes in SLSA statement). (no new flag — recorded automatically) (see [pokkum-verify-concept.md](concepts/archive/pokkum-verify-concept.md))
- [x] M1/M2: Stage recorder, bisection core, and attestation-check mode (`pokkum verify --no-rebuild`). Provenance resolution and Cosign/Sigstore signature and SLSA attestation validation are fully functional and tested against real in-memory OCI registries (`internal/adapters/provenance/resolver.go`). (see [pokkum-verify-concept.md](concepts/archive/pokkum-verify-concept.md))
- [x] M3: `layerdiff` component, L3 explanation, and `pokkum repro doctor` diagnostics. (new flag: `--fast` for static-checks-only, no build) (see [pokkum-verify-concept.md](concepts/archive/pokkum-verify-concept.md) & [pokkum-repro-doctor-concept.md](concepts/archive/pokkum-repro-doctor-concept.md))
- [x] M4: `pokkum verify --rebuild` (L1/L2 compare) with `--perturb` mode, K8s + CI ergonomics. (new flags: `--against <path>`, `--expect-source <repo>@<ref>`, `--all-platforms`) (see [pokkum-verify-concept.md](concepts/archive/pokkum-verify-concept.md) & [pokkum-repro-doctor-concept.md](concepts/archive/pokkum-repro-doctor-concept.md))

## v1.0: MVP Launch (Supply Chain Hardening & Production Readiness)

_The shippable minimum viable product for enterprise-grade adoption._

### Supply Chain

- [x] Trusted-Builder Mode: SLSA L3 via an isolated CI job (GitHub Actions provenance generation). (no new flag — a CI workflow concern, not a CLI flag)
- [x] CVE Scanning Integration (`pokkum scan`): Run vulnerability scanning against image, tarball, or directory and fail pipeline above severity threshold. (new flag: `--fail-on=critical`)
- [x] Toolchain CVE Awareness: OSV.dev advisory lookups keyed on embedded Bun and SvelteKit versions. (new flag: `pokkum scan --toolchain`, extending `scan`)
- [x] Base Image Signature Verification: Two modes — static-key Cosign verification for custom/self-signed bases (`POKKUM_BASE_IMAGE_PUBKEY`), and keyless Sigstore verification (Fulcio + Rekor) for stock `distroless`/`chainguard` presets by default. (new flags: `--base-verify-mode {auto|keyless|static-key}`, `--base-keyless-identity`, `--base-keyless-issuer`, `--sigstore-trusted-root`, `--no-verify-base` opt-out)
- [x] Secret-Inlining Guard: Build-time entropy/pattern scan to prevent leaked secrets in layers. (new flag: `--allow-secret-pattern` escape hatch for false positives)
- [x] Base image digest pinning + automated update PRs (Renovate/Dependabot-style). (reuses `pokkum base update --preset <name>` from the v0.2 lockfile item; update-PR half is a bot, not a flag) (see [pokkum-lock-concept.md](concepts/archive/pokkum-lock-concept.md))
- [x] Standard OCI Annotations: `org.opencontainers.image.revision`/`.source`/`.version`/`.created` auto-populate on every build by default — no flag needed, and no opt-out flag exists. `revision`/`source`/`version` come from git (commit SHA, remote URL, `git describe`) or CI env vars; `created` is set to the exact same `SOURCE_DATE_EPOCH`-resolved timestamp used for layer mtimes and the image config elsewhere in the build (not resolved independently, so it can't disagree with what's actually in the image) and is left unset — not fabricated from `time.Now()` — when that timestamp can't be determined. `--image-label key=value` still works for explicit overrides (checked first, so an explicit value always wins) and any other annotation. Outside a git repository (or without `git` on `PATH`), `revision`/`source`/`version` are silently absent — no warning is printed. (new flag: `--image-label key=value`, repeatable — matches `ko build --image-label`)

### Cluster-side hardening

- [x] `NetworkPolicy` generation restricting egress and ingress limited to expected ports, `podSelector` scoped to the workload's own Pod-template labels. (new flags: `--network-policy` / `--no-network-policy` on `resolve`/`apply`)
- [x] Resource `requests`/`limits` and a `PodDisruptionBudget` by default in generated manifests, selector-scoped to the workload rather than the whole namespace — skipped entirely, not emitted unscoped, when no workload labels can be found. (new flags: `--resource-defaults` / `--no-resource-defaults` on `resolve`/`apply`)
- [x] `readOnlyRootFilesystem: true` where feasible (supervisor + compiled binary). (no new flag — folds into `--security-context`)
- [x] Readiness Drain on SIGTERM: Supervisor holds `/readyz` at 503 while the app drains. (no new flag — bounded by the existing `POKKUM_SHUTDOWN_TIMEOUT` env var)
- [x] Secrets via `envFrom` referencing Kubernetes `Secret`/external-secrets, never baked into image layers. (no new flag — expressed in the manifest itself)

### Build integrity

- [x] Hermetic builds: No network egress during the compile step (SLSA L3 requirement). (new flag: `--hermetic`, opt-in)
- [x] Multi-registry support with per-registry auth chains via a `docker config.json`-style file keyed by registry hostname, merged ahead of the default keychain (self-hosted and any registry with static credentials in the file). No ECR/GCR/ACR-specific credential-helper invocation by design — Pokkum stays zero-dependency rather than vendoring cloud-provider SDKs; the standard `docker config.json` format is the deliberate boundary. (new flag: `--registry-config=<path>`)
- [x] Ephemeral test registry (`pkg/registry.NewServer()`) wired into CI for integration tests. (no new flag — test infra only)

### Operational maturity

- [x] Version pinning of `pokkum` itself in generated manifest annotations. (no new flag — folds into `--image-label`/auto-annotations above)
- [x] A GitHub Action wrapping the CLI (mirroring `setup-ko`). (no new `pokkum` flag — Action inputs like `version`/`token`, mirroring `setup-ko`)
- [x] Rollback support (`pokkum rollback` reading from declarative manifest annotations `pokkum.dev/previous-image`). (new subcommand: `pokkum rollback -f <manifest> [--to=<ref>]`, reusing `-f`/`--file` from `resolve`/`apply`; `--to` is optional and defaults to `pokkum.dev/previous-image` annotation)
- [x] Signed Self-Distribution (`pokkum upgrade`): Signature verification of release artifacts and binary self-updates via `ports.ReleaseVerifier` and `cosign`. (new subcommand: `pokkum upgrade`, new flags: `--check`, `--version`, `--offline`, `--key`)

## Post-v1.0 Enhancements (Completed & Shipped)

The post-v1.0 milestones addressed critical supply chain verification, CVE gating, registry interoperability, and developer adoption capabilities:

### Supply Chain & Verification Rigor (Tier 0)
- [x] **Real Provenance & DSSE Envelope Verification**: `internal/adapters/provenance/resolver.go` pulls real manifests, verifies Cosign signatures (`<repo>:sha256-<hex>.sig`) via static-key or keyless Sigstore, extracts and verifies in-toto DSSE envelopes and SLSA v1.0 statements (`<repo>:sha256-<hex>.att`), enforces `--expect-source` assertions, and conducts toolchain skew analysis.
- [x] **Bit-for-Bit Image Comparator**: `internal/adapters/comparator/comparator.go` performs genuine L1 (exact manifest hash), L2 (semantic uncompressed DiffIDs and config), and L3 (layer-by-layer tar stream file diffs with root cause analysis) comparisons between remote images and local rebuild tarballs.
- [x] **Attestation & Verification CLI**: `cmd/pokkum/verify.go` supports `--registry-config`, `--no-rebuild`, and `--against <tarball>`, reporting accurate cryptographic verdicts and exit codes.

### Security & Base Image CVE Build Gate (Tier 1)
- [x] **Real OS & Toolchain Vulnerability Scanning**: `pokkum scan` pulls and enumerates image contents via `syft` and queries OSV.dev batch API (`/v1/querybatch`) for real OS-package and ecosystem advisories. Fails closed on incomplete scans by default (`--allow-incomplete` opt-out).
- [x] **Base Image CVE Reactivity & Build Gate**: `pokkum build` actively scans the resolved base image digest against OSV.dev, logging warnings by default and failing builds when configured with `--fail-on-cve=<severity>` (or `POKKUM_FAIL_ON_CVE`). Persists `last_scanned_at`, `vulnerabilities_count`, and `max_severity` into `pokkum.lock`. An active gate combined with `--offline`/`--hermetic` used to silently skip the scan with zero log output — fixed to fail closed (reusing the `--allow-incomplete` escape hatch) unless the operator explicitly acknowledges the scan can't run offline; see `TestPipeline_BaseImageCVE_Gating`'s offline/hermetic subtests.
- [x] **Base Image Escrow / Mirroring**: `pokkum base update --mirror-registry=<repo>` mirrors base images/indexes and their Cosign `.sig` tags to a project-controlled registry with automatic fallback. Resolving with `--mirror-registry` and `VerifySignature: true` verifies `docker-reference` claims against the upstream repo recorded in `pokkum.lock` (`entry.Ref`), preserving signature authenticity.

### Enterprise Registry Interoperability (Tier 2)
- [x] **Credential-Helper Invocation**: `--registry-config` dynamically resolves credentials via `credHelpers` and `credsStore` by executing `docker-credential-*` binaries (e.g., ECR, GCR, OSXKeychain) with in-memory caching and fallback to static `auths` blocks, supporting cloud registries with zero new external SDK dependencies.

### Adoption & Runtime Safety (Tier 3)
- [x] **Migration Codemod (`pokkum adopt`)**: Auto-converts `@sveltejs/adapter-node`, `adapter-vercel`, `adapter-auto`, and `adapter-cloudflare` projects to native Pokkum compilation defaults with AST/regex config rewrites, `.pokkumignore` bootstrapping, and optional legacy Dockerfile removal (`--dry-run`, `--remove-dockerfile`).
- [x] **Multi-Generation Rollback History**: `pokkum rollback` supports arbitrary historical rollback depths via `pokkum.dev/image-history` manifest annotations with timeline inspection (`--list`) and generation selection (`--generation=<n>`, `-g <n>`).
- [x] **Image Provenance Timeline (`pokkum history`)**: Inspects published OCI manifests and extracts standard `org.opencontainers.image.*` provenance annotations (git commit, repository, build timestamp, version).
- [x] **Live Cluster Annotation Inspection**: `pokkum apply` queries live cluster state (`kubectl get <kind>/<name> -n <ns> -o json`) before manifest resolution to seed `pokkum.dev/image-history` and active images, enabling multi-generation history accumulation across independent CLI runs on static `pokkum://` templates (`--cluster-inspect`, `--no-cluster-inspect`). Every kubectl failure (unreachable cluster, RBAC denial, malformed kubeconfig) used to be folded into the same "workload not found" empty state at Debug-only log level, silently resetting rollback history with no operator-visible signal. Fixed: only a genuine `(NotFound)` StatusReason is treated as fresh; every other failure surfaces as a Warn-level log line via `ResolveResult.ClusterInspectionWarnings`.
- [x] **Runtime Environment Contract**: Declares required runtime environment variables in OCI image annotations (`pokkum.dev/required-env`) and embeds contract into runtime config, enforced by PID-1 supervisor (`/pokkum/init`) to fail-fast on startup if any are missing (`--require-env=KEY1,KEY2` on `build`).

### Adoption & Monorepo Scale (Tier 4)
- [x] **Monorepo Affected-Detection (`--since`)**: `pokkum resolve` and `pokkum apply` accept `--since=<git-ref>` to diff each `pokkum://` project's source tree against a base commit (`internal/adapters/gitutils` — `git diff --name-only <ref> -- .` plus `git status --porcelain` for untracked/staged/deleted changes). An unaffected project with a known prior digest — read from the manifest's `pokkum.dev/current-image` annotation, or from live cluster state when configured — **skips compilation and packaging entirely** and reuses that digest in the emitted manifest (logged as `reused unaffected image reference`). If no prior digest is known, or the project is affected, it falls through to a normal build so every emitted reference is a real `repo@sha256:…`. `--since` is fail-closed: an unknown ref or a git failure fails the resolve rather than silently building or silently skipping. See `internal/ports/k8s.go` (`AffectedDetector`, `ResolveRequest.Since`, `Reference.Skipped`) and `cmd/pokkum/k8s.go`.

---

## Architectural & Performance Optimizations

*Focusing on build speed, CLI footprint, and container image size reduction.*

### 1. Build Speed & Throughput
- [x] **Pre-Calculated Immutable Layers**: Cache pre-compressed `bun` and `pokkum-init` blobs locally to skip ~100MB of tarring and compression per build.
- [x] **Single-Pass Layer Hashing & Streaming**: Compute uncompressed DiffID and compressed Digest simultaneously using multi-threaded compression (`pgzip` or `zstd`) to significantly speed up layer generation. The digest/diffID computation itself was independently adversarially verified as correct, deterministic, and bit-reproducible across GOMAXPROCS settings (byte-identical to the old fully-buffered path). Two real resource leaks were found and fixed: intermediate temp files relied solely on a GC finalizer that a CLI process normally exits before running (hundreds of MB/build orphaned in `/tmp`), and the zstd decoder was never released due to an `io.Closer` type-assertion mismatch — both fixed with deterministic cleanup (see `TestBuildSinglePassLayer_TempFileCleanup`, `TestUncompressed_ZstdDecoderClosed`).
- [x] **Overlapped Async Pipeline Stages**: Run base image resolution, signature verification, and module inspection in parallel background goroutines during the SvelteKit build phase. Base image *digest* resolution stays ahead of the remote-cache check (the cache key needs it), but signature verification and native-module inspection now run concurrently with `Compiler.Prepare` via `errgroup.WithContext`, gated on a confirmed cache miss — a cache hit skips both entirely since neither has anything left to gate once nothing is being built from source or from the base image. A failure in either concurrent check cancels the in-flight SvelteKit build (tearing down the `bun` subprocess tree, not just abandoning a goroutine) before publishing is ever reached. `--dry-run` keeps synchronously verifying the signature and inspecting native modules first, matching pre-change fail-fast behavior.
- [x] **Registry Push Optimizations**: Parallel layer uploads over HTTP/2 (`remote.WithJobs`, wired via `remoteConfig.Jobs`) and cross-repository blob mounting (`mount=`/`from=` query parameters) for zero-egress base layer copies — `internal/adapters/registry/mount.go`'s `mountObserver` sniffs the wire (never the response body) to classify each blob's outcome as mounted/streamed/already-present without any production-code hook into go-containerregistry's unexported writer internals, and `Push` logs `mount_layers_mounted`/`_declined`/`_already_present` on every successful push. Proven end-to-end in `internal/adapters/registry/push_test.go` against a repo-scoped mount-emulating test registry (`mount_test.go`): same-host mount success with zero bytes re-uploaded (`TestPush_CrossRepoMount_ZeroEgress`), graceful fallback when the claimed source blob is genuinely absent (`TestPush_CrossRepoMount_FallbackOnAbsentBlob`), graceful fallback across two independent registry hosts — the case that actually proves the optimization degrades safely rather than breaking (`TestPush_CrossRepoMount_CrossRegistryRejected`) — and correct handling of an explicit registry-side mount rejection exercising go-containerregistry's retry-without-mount path with nothing double-uploaded (`TestPush_CrossRepoMount_RegistryRejectsMount`). Along the way, fixed a real bug in the test harness itself (not production code): upstream's own repo-path arithmetic (`pkg/registry/blobs.go`) computes a different repo string for chunked-upload PATCH/PUT requests than for reads, so the harness's repo-scoped blob store stored real pushed blobs under `"<repo>/blobs"` while reads looked them up under plain `"<repo>"` — every non-mounted blob would appear to vanish. Fixed with `normalizeBlobRepo`; see `TestMountAwareTestRegistry_RealWriteThenReadAgreeOnRepo` and `Lessons.md`. The user-facing `--push-concurrency` flag (default 0, defers to adapter's hardcoded 4) is wired through `cmd/pokkum/build.go` → `core.BuildRequest.PushConcurrency` → `ports.PushRequest.Concurrency` → `remoteConfig.Jobs`; documented in `Vocabulary.md` section 3. Fixed HTTP/2 negotiation for `--insecure` pushes against self-signed-TLS registries by cloning from `remote.DefaultTransport` (preserving `ForceAttemptHTTP2`, proxy support, connection pooling) instead of bare `&http.Transport{}` literals in `internal/adapters/registry/registry.go`'s `defaultTransport`/`insecureTransport` — plain `http://` insecure targets still don't get HTTP/2 (a net/http limitation, not something this fixes). Confirmed pre-existing correct behavior: base images resolved via `--mirror-registry`/`pokkum.lock`'s `MirrorRef` produce mountable layers pointing at the mirror repo (not upstream), so mount-based zero-egress pushes engage the most common production case; separately, go-containerregistry intentionally suppresses cross-repo mount attempts when the push target is Docker Hub and source isn't also Docker Hub (documented upstream API workaround), so `mount_layers_mounted` staying low/zero on Docker Hub pushes is expected, not a defect — verified in `TestResolve_MirrorRef_LayersAreMountableFromMirrorRepo` and `TestPush_DockerHubMountSuppression_IsRealAndAppliesToOurPushPath`. Two pre-existing anti-patterns (bare `&http.Transport{}` literals) previously identified as follow-ups in this same entry — `internal/adapters/baseimage/resolver.go`'s `insecureTransport` and the three sites in `internal/adapters/remotecacheutils/remotecacheutils.go` (signature fetch, `CheckRemoteCache`, `ReconcileTags`) — have since been fixed by centralizing the clone-based transport builder in the shared `internal/adapters/transportutils` package — `transportutils.CloneDefaultTransport(tlsConfig)` for custom configs and a single process-wide `transportutils.InsecureTransport()` singleton (lazily built via `sync.OnceValue`, so connection pooling is shared across all one-shot insecure registry calls rather than per-call) — which `registry.go`, `baseimage`, and `remotecacheutils` all now use; `ForceAttemptHTTP2`, proxy support, and the 50-conn idle pool are preserved on every insecure path, regression-guarded by `TestCloneDefaultTransport_*`, `TestInsecureTransport_IsSharedSingleton`, `TestInsecureTransport_PreservesRemoteDefaultTransportTuning` (baseimage), and the existing `TestTransports_PreserveRemoteDefaultTransportTuning` (registry).

### 2. CLI Binary & Footprint Reduction
- [x] **Targeted Scanner vs Monolithic Syft**: Replace the monolithic `anchore/syft` dependency with lightweight, zero-dependency parsers for `dpkg`, `apk`, and `package.json` to cut CLI binary size by ~50MB. *(⚠️ Requires long-term maintenance: Must track changes in Debian/Alpine package database formats and NPM ecosystem lockfile specs)* Image/tarball scanning (`pokkum scan <image>`) had a confirmed bug: it read only the `dependencies`/`devDependencies` maps out of vendored `package.json` files, never their own `name`/`version` — so it never actually cataloged the real installed npm packages Pokkum ships, undermining CVE lookups against the built image. Fixed to record each vendored package's own declared identity; see `TestExtractImagePackages_VendoredPackageJSON`.
- [x] **Compressed Embedded Supervisor Binaries**: Pre-compress `pokkum-init` binaries with `zstd` before `go:embed` and decompress on-the-fly, saving ~8MB.
- [x] **Compiler Build Optimization Flags**: Enforce `-trimpath -ldflags="-s -w"` in release pipelines for significant binary size reduction. (no new flag — enforced in the `Makefile` `build`/`supervisor` targets, `.goreleaser.yaml`, and the `slsa-builder.yml` SLSA L3 path; `-X main.version/commit/buildDate` are set in all three so release binaries keep real version metadata; `scripts/check-build-flags.sh` (wired into `make verify` as Step 0) guards against the flags silently being dropped)

### 3. Container Image Size Reduction
- [x] **Vendor Layer Pruning**: Automatically strip `*.map`, `*.d.ts`, and test/doc files from `node_modules` during bundling for a 15-35MB reduction. *(⚠️ Requires long-term maintenance: The exclusion rules must be continually updated as NPM ecosystem packaging norms and SvelteKit runtime dependencies evolve)* The doc/metadata patterns (README, LICENSE, ...) were bare wildcard prefixes that also matched real runtime source files starting with the same word (`readme.js`, `license-checker.js`), silently deleting them from the vendor layer — a would-be module-not-found crash at container startup. Fixed with an exact-basename-plus-known-extension match (`isDocFile`); see `TestIsJunk_DocFilesNotFalsePositive`.
- [x] **ELF Native Addon Stripping**: Run `strip --strip-unneeded` on native `.node` addons to discard debug symbols and unneeded relocations. On any host without an ELF-capable strip tool on `$PATH` (e.g. plain macOS), the failure fell through silently and the result was discarded — native addons shipped completely unstripped with zero warning the feature had done nothing. Fixed to surface a `Warn`-level log naming the skipped files; see `TestStripDirectory_NoWorkingStripTool_SurfacesSkipped`.
- [x] **Build-Time Static Asset Pre-Compression**: Pre-compress `/app/client` assets with Brotli (`.br`) and Zstandard (`.zst`) alongside `.gz` so the server serves them immediately without runtime CPU compression overhead. Sidecar generation only checked whether a `.gz`/`.br`/`.zst` file already existed, never whether the source had since changed — a reused output directory could serve stale, mismatched compressed content indefinitely. Fixed with an mtime-based staleness check; see `TestPrecompressFile_RecompressesStaleSidecar`.

### 4. System Efficiency & Remote Caching
- [x] **Composite Remote OCI Input Caching & Anti-Poisoning Verification**: Skip builds entirely (sub-100ms) if an image already exists in the registry for a given hash of `(source + lockfile + baseDigest + bunVersion + platforms + flags)`. Fully closed all known cache integrity gaps: (1) fixed-length digest framing prevents source-tree/lockfile second-preimage framing collisions; (2) input hash comprehensively covers the entire runtime, telemetry, compile, and SBOM contract; (3) `ReconcileTags` failure propagation prevents false cache hits; and (4) **Cryptographic Signature Verification Before Tag Promotion**: before promoting release tags or accepting a cache hit, `remotecacheutils` cryptographically verifies Cosign static-key or Sigstore keyless signatures on the candidate cache-hit image (`<repo>:<alg>-<hex>.sig`). Unverified or poisoned cache entries are cleanly rejected, seamlessly falling back to a full compilation from source without permitting unverified code promotion, and signed builds can safely participate in build-skip caching when candidate images carry verified signatures matching trusted credentials. The repo-claim comparison in the signature-payload check (`checkSimpleSigningClaims`) originally used a suffix match rather than an exact match — a payload claiming `"evil.com/" + expectedRepo"` was wrongly accepted as matching `expectedRepo`. For the keyless path this check is the *only* thing standing between a validly-signed-by-the-trusted-identity payload for a different repo and a false accept (sigstore-go's `Verify` proves identity + payload integrity, never which repo the payload claims to describe); the static-key path is separately protected by `cosign.Signer.Verify`'s own exact-match check, so this was a real gap specifically in the keyless mode. Fixed to exact match, matching the convention already established in `internal/adapters/baseimage/resolver.go`. See `internal/adapters/remotecacheutils/remotecacheutils_test.go` (`TestCacher_Check_KeylessVerification_SuffixRepoClaimRejected`) and `internal/core/pipeline_test.go`.
- [x] **`sync.Pool` Buffer Recycling**: Eliminate repetitive large slice allocations during tarball generation to reduce GC pressure and memory spikes.
- [x] **Fast Sub-Millisecond Supervisor Startup**: Parallelize Bun child process fork/exec with the `/healthz` HTTP probe binding in `pokkum-init`. The child executable is resolved up front (`resolveChildPath` → `Supervisor.WithChildPath`) so `start()` forks immediately with no `exec.LookPath` on its critical path, while the probe server binds `/healthz` on its own goroutine — the listener bind and the Bun fork/exec overlap instead of serializing. Regression-guarded by `startup_test.go` (`TestProbeServesWhileChildIsRunning` proves `/healthz` serves live state while the child is still running, and `TestResolveChildPathFailureMapsToStartCodes` pins the 126/127 exit-code mapping for the pre-resolved failure path).

---

## Recommended Next Steps (Prioritized)

Prioritized backlog for ongoing development:

### 0. Layered-Strategy Real-Build Correctness (Critical Priority — Beta-Blocking)
- **Problem**: discovered 2026-08-17 by running real `bun run build` against a freshly `sv create`-scaffolded project (not a checked-in fixture with the adapter already hand-configured) — the exact workflow a new user follows first. Four real bugs, found empirically, not by code review alone (the fourth was found while verifying the fix for the other three, by running the actual `core.Build` pipeline end to end rather than just `Prepare` in isolation):
  - [x] **`assets.generated.ts` normalization ran for `StrategyLayered`** (FIXED 2026-08-17): `internal/adapters/bunexec/compiler.go`'s post-build step unconditionally normalized `assets.generated.ts` — an artifact exclusively produced by `@jesterkit/exe-sveltekit` (the `exe` strategy's adapter) — for every non-static strategy, including `layered`. `@sveltejs/adapter-node` never produces that file, so **every real `--strategy=layered` build (the default) against its own documented, correct adapter failed** with "no such file or directory," even though the underlying SvelteKit/Vite build succeeded cleanly. Fixed by scoping the normalization to `StrategyExe` only. The bug shipped and stayed hidden because the golden-master test added the same day (`TestPrepare_StrategyDispatch`) fabricated a fake `assets.generated.ts` in its "layered" fixture to get past this exact line — satisfying the bug instead of catching it; the fixture now omits that file, matching real adapter-node output, and genuinely regression-guards this. See `Lessons.md`'s 2026-08-16 entries and `mem:self_review_checklist` row 11.
  - [x] **Zero-Config Auto-Injection (v0.2) has no effect on real builds** (FIXED 2026-08-17, Option C): `PrepareVirtualConfig` writes a transformed `svelte.config.js` to `.pokkum/` and sets `POKKUM_AUTO_INJECT=1`, but nothing ever reads either — the real `bun run build` always ran against the real, unmodified `svelte.config.js`/`vite.config.ts`. Per `concepts/zero-config-injection-concept.md`'s recommendation, shipped **Option C** (detect-and-fail-clearly), not Option B (a generated wrapper actually making injection work, still deferred) or Option A (rejected). `sveltekitutils.EffectiveAdapterConfigured`/`ViteConfigOverridesSvelteConfig` (`internal/adapters/sveltekitutils/project.go`) determine which file SvelteKit will actually read; `bunexec.Compiler.Prepare`'s `checkEffectiveAdapter` (`internal/adapters/bunexec/compiler.go`) fails fast — before any subprocess is spawned or `.pokkum/` is written — with a message naming the exact file to fix, wrapping the new `core.ErrAdapterMisconfigured` sentinel. **v0.2's "Zero-Config Auto-Injection" entry above is corrected by this note**: it still does not deliver zero-config (that's Option B, still not built); what it delivers today is a fast, actionable error instead of a silent wrong build or a confusing failure several minutes later.
  - [x] **Current `sv create` scaffolds configure the adapter via `vite.config.ts`, not `svelte.config.js`** (FIXED 2026-08-17, same change as above): confirmed empirically — a real `bunx sv create` scaffold (`sv@0.17.0`/`@sveltejs/kit@2.63.0`–`2.70.2`) generates **no `svelte.config.js` file at all**; the adapter is configured purely via `vite.config.ts`'s `sveltekit({ adapter: adapter() })` call, and a default (no-add-on) scaffold imports `@sveltejs/adapter-auto`, not the strategy's actual target adapter. `EffectiveAdapterConfigured` handles this directly: when `vite.config.ts`/`.js` passes non-empty options to `sveltekit()`, it — not `svelte.config.js`, present or not — is authoritative, matching SvelteKit's own documented behavior (confirmed by a real `bun install`/`bun run build` printing "svelte.config.js is ignored when options are passed via your Vite config" and then genuinely building with the Vite-configured adapter).
  - [x] **`patchPrerenderedHandler`'s "real fixture" regression tests exercised the wrong artifact** (FIXED 2026-08-17): confirmed empirically that real bundled `build/handler.js` (`@sveltejs/adapter-node@5.5.7`) is a thin re-export barrel (`export { h as handler } from './server/chunks/handler-<hash>.js';`) — the actual `path.join(dir, 'prerendered')` pattern (an exact, unchanged match of one of `patchPrerenderedEnv`'s existing 8 patterns) lives in that content-hashed chunk file, not in `handler.js` itself. The matcher wasn't wrong, it was reading the wrong file. Fixed in `internal/adapters/bunexec/prerendered_patch.go`: `patchPrerenderedEnv` now tries a direct match in `handler.js` first (unchanged), then — only on no-match — parses `handler.js`'s re-export statement, resolves the referenced chunk file, and retries there; a genuine no-match in both stays a hard failure. New real-derived fixtures under `testdata/adapter-node/bundled-real/`; the old `npm pack`-sourced `v3`/`v5` fixtures are kept (still real, still useful) but their doc comments now correctly state they test the pre-bundling template, not build output. See `Lessons.md`'s 2026-08-16 entry and `mem:self_review_checklist` row 12.
  - [x] **(Found and fixed during verification of the above, 2026-08-17) `Preflight` unconditionally required `svelte.config.js` to exist**: even after the two fixes above, every real `sv create` project (which, per the second bullet, ships no `svelte.config.js` at all) still failed at `Preflight` — a separate, strategy-agnostic check that runs before `Prepare` in the real `core.Build` pipeline — with `"svelte.config.js not found: sveltekit project not found"`, before `Prepare`'s own fix ever got a chance to run. Only caught by actually running `core.Build` end to end against a real project (`tests/integration/layered_prerendered_e2e_test.go`) — none of the fixes above were exercised through the full pipeline until this test was written. Fixed in `internal/adapters/bunexec/compiler.go`'s `Preflight`: a missing `svelte.config.js` is no longer, by itself, `core.ErrProjectNotFound` — only a genuine read failure (not "does not exist") is. `Preflight`'s existing package.json-dependency fallback for adapter detection already tolerated this once the hard file-existence gate was removed.
- **Status**: all four items above are fixed and regression-tested (`internal/adapters/sveltekitutils/project_test.go`, `internal/adapters/bunexec/compiler_test.go`, `internal/adapters/bunexec/compiler_strategy_test.go`, `internal/adapters/bunexec/prerendered_patch_test.go`, `tests/integration/layered_prerendered_e2e_test.go`). The one thing this section does **not** claim: true zero-config injection (Option B) remains unbuilt and is tracked separately — today's fix makes a misconfigured project fail fast and clearly, not build itself correctly with no user action. A real `--strategy=layered` build with a prerendered route was independently confirmed via a real `bun install`/`bun run build` against `testdata/fixtures/sveltekit-adapter-node` up through `Prepare`; `tests/integration/layered_prerendered_e2e_test.go`'s full-pipeline assertions currently skip on an unrelated, separately-flagged issue (a stale pinned checksum for the embedded `bun` runtime binary in `internal/adapters/bunruntime`, unrelated to anything in this section) rather than exercising the packaged image's tar contents — see that test's skip message and the flagged follow-up.

### 1. Static / Prerendered Page Optimization (Medium Priority)
- **Problem**: Fully static or predominantly prerendered SvelteKit sites still incur Bun runtime memory overhead and layer distribution size.
- **Solution**:
  - Extract prerendered static assets to a dedicated slim OCI layer with immutable Cache-Control metadata (`/app/prerendered`, layered strategy; adapter-node handler patched to honor `POKKUM_PRERENDERED_DIR`).
  - Add a `--static` mode compiling purely static sites onto a minimal libc-free `cgr.dev/chainguard/static` image served by the embedded statically-linked `pokkum-static` Go file server (`/pokkum/static`, PID 1) with zero JavaScript/Bun runtime overhead. `pokkum-static` implements Range requests, strong ETags, and Content-Encoding negotiation against the `.gz`/`.br`/`.zst` sidecars `precompressutils` generates.
- **Flags/Interface**: `--static` on `pokkum build` (shorthand for `--strategy=static`; conflicts with an explicit `--strategy` other than `static`; defaults the base to `chainguard/static` under the `chainguard` preset — not `distroless`, fixed 2026-08-16 after a signature-identity mismatch was found; see item 3 above for the dedicated-preset follow-up — when no `--base`/`--hardened` is given).

**[x] Status: Done** — full static + prerendered pipeline shipped and verified.

**Follow-ups (not shipped with the feature — see `paranoid-testing-guide.md` §22 for how to exercise them by hand):**
- [x] **Static end-to-end integration fixture**: `tests/integration/static_e2e_test.go` (`TestFixtureDrivenE2E_Static`) drives a real multi-platform `StrategyStatic` build through the full `core.Build` pipeline and asserts no Bun/supervisor layer, correct base-image threading, and correct entrypoint/env. `supervisor/cmd/pokkum-static/integration_test.go` proves `pokkum-static` serves real files — built with production `precompressutils` compression, not faked bytes — with working Range/ETag/Content-Encoding against the genuine `.gz/.br/.zst` sidecars, including immutable-asset cache headers. A companion golden-master suite (`tests/integration/strategy_e2e_test.go`, `internal/adapters/packager/packager_strategy_test.go`, `internal/adapters/bunexec/compiler_strategy_test.go`) now pins layer/entrypoint/env behavior across all 3 `BuildStrategy` values, and caught two real bugs in the process: `StrategyLayered` images built through the real pipeline previously got an unrunnable `StrategyExe`-shaped entrypoint (`core.Build`'s `Normalize()` pre-defaulting `Runtime.Entrypoint` before the strategy was known — fixed in `internal/adapters/packager/packager.go`), and multi-platform `StrategyLayered`/`StrategyStatic` builds collided on a zero-value platform map key (fixed in `internal/core/pipeline.go`). See `Lessons.md`'s 2026-08-16 entries.
- [x] **Adapter-node handler-version hardening**: verified against real `@sveltejs/adapter-node@3.0.3` and `@5.5.7` output (`testdata/adapter-node/{v3,v5}/handler.js`, sourced via `npm pack` and documented in `testdata/adapter-node/README.md`), regression-tested in `internal/adapters/bunexec/prerendered_patch_test.go`. Correcting this bullet's prior text: the matcher already hard-fails the build (not warn-and-continues) on zero matches. Both real fixtures showed exactly one match with no coincidental collision against any of the other 7 known patterns, so the match-count guard against a false-positive was evaluated and found not currently warranted — the finding is pinned by the new regression tests so a future re-sourced fixture that does introduce ambiguity fails loudly instead of silently patching.
- [x] **SPA-fallback for `adapter-static` (implemented, 2026-08-17, Option B — opt-in, runtime-configured path)**: when a static site uses a `fallback` page (SPA mode), `pokkum-static` serves the fallback for unknown routes on a purely static image. **Decision: opt-in fallback.** The server keeps serving a plain `404` for unmatched routes by default (preserving honest 404s + the currently-tested contract), and an opt-in SPA-fallback mode serves the fallback shell with `200` when enabled. The fallback is a **file path configured at runtime** (`POKKUM_STATIC_FALLBACK` env / `-fallback` flag, default empty = current 404 behavior), not a hardcoded `200.html` guess — the packager is the source of truth for what `adapter-static` emitted, and the staging + routing are driven by that (`sveltekitutils.StaticFallbackFilename` reads the `fallback` option from `svelte.config.js`; the static `bunexec.Prepare` verifies the exact file was emitted in the client staging, hard-failing if configured-but-not-emitted — it is never silently dropped). **Discovery of the mode**: a one-per-process `Warn`-level server log line is emitted on the first `404` while `POKKUM_STATIC_FALLBACK` is unset, pointing at `Vocabulary.md`/docs (mirrors the `port == probePort` warn-once pattern); the 404 response body stays clean for real/public consumers, so no dev-marker HTML comment is injected into production responses. **Implemented + tested:** runtime fallback config + serve-with-200-on-miss (GET/HEAD only, with the same ETag/Content-Encoding negotiation as other served files), adapter-static output-root fallback staged into the image off the served roots (file never silently dropped), rebuilt + re-embedded `pokkum-static` binaries (`make static-server`), and tests `TestStaticServer_Fallback_OnMiss`/`_NotForExistingRoute`/`_MethodNotAllowed`/`_TraversalRejected`/`_OutsideRootRejected`/`_NegotiationWithSidecar`/`_DefaultStill404`/`_WarnOnceOn404WhenUnset` (server), `TestParseConfig_Fallback*`, `TestStaticFallbackFilename` (sveltekitutils), `TestPrepare_StaticFallback_*` (bunexec), `TestBuild_StrategyStatic_FallbackEnv` (packager), and the SPA-configured `--static` integration test `TestFixtureDrivenE2E_Static_SPAFallback` (asserting the unknown route serves the shell via image env + staged layer).

### 2. Layered-Strategy Runtime Hardening (Medium-High Priority)
- **Problem**: `--strategy=layered` (the default since v0.3) ships stock `bun`, which exposes its full CLI via `BUN_BE_BUN=1` (and, per open upstream bugs, sometimes without the env var at all) — `BUN_BE_BUN=1 /app/server evil.js` runs arbitrary scripts on any host with an existing exec primitive. This is the recommended-but-never-built half of the v0.3 layer-caching migration's own hardening analysis; only the `readOnlyRootFilesystem` piece shipped. Unlike the other items in this section, this is a gap in the **default** build path every user already gets, not an opt-in feature.
- **Solution** (see `concepts/archive/pokkum-layer-caching-concept.md` §5.2 for full option analysis):
  - **Option A — compiled stub launcher (SPIKE DONE 2026-08-17, full build DEFERRED)**: ship a tiny `bun build --compile` executable whose only code is `await import("/app/server/index.js")`, path compiled in rather than read from argv/env.
    - **Spike result (confirmed with real `bun` 1.3.14)**: `bun build --compile` CAN produce an exe that runtime-imports the live `/app/server/index.js` **including TS transpilation** — **provided** the import spec is expressed so the bundler does not constant-fold it (e.g. `const p = "/app/server/" + "index.js"; await import(p)`, or built via a helper/`new Function`). A statically-analysable literal `import("/app/server/index.js")` gets **bundled in** at compile time, which would bake the server code into the stub layer and break the layer-caching premise. So Option A is technically feasible with the non-foldable form.
    - **Decision (deferred)**: full implementation is **deferred**. It requires adding `bun` as a CLI *build-time* tool (to compile the stub per Bun version × arch), cross-compilation plumbing into `internal/adapters/bunruntime` (which today downloads a pinned `bun`), and per-arch stub embedding. Its security value is **parity** with v0.1's exe — the concept doc is explicit that the `BUN_BE_BUN` surface is "no worse, no better" than a compiled exe — i.e. it does **not** strictly close the surface, and the already-shipped Option C already restores tamper-evidence. Recorded here so the decision and its rationale are recoverable; revisit if build-time `bun` becomes acceptable or a true surface reduction (Option D) is wanted.
  - **Option C — supervisor startup attestation (IMPLEMENTED, 2026-08-17)**: built + tested. The packager computes a deterministic SHA-256 root digest over the authoritative post-processing `/app` tree (records emitted per layer by `BuildDirectoryTreeLayerWithPruning`, so pruning/sidecar/strip already applied; shared algorithm in `internal/adapters/attestutils`) and stamps it as `POKKUM_ATTESTATION_DIGEST` into the image config env for `--strategy=layered`. `pokkum-init` mirrors the algorithm (`supervisor/cmd/pokkum-init/attest.go`, no `internal/ports` import) and re-derives the digest from the live `/app` tree before exec, refusing to start (exit 126) on mismatch — tamper-evidence without depending on cluster-level readonly-fs policy. **Design deviation from the concept doc's literal "bake into the `/pokkum/init` layer":** the expected value is delivered via image-config **env**, not by adding a file to the supervisor layer, so the immutable supervisor-layer cache is preserved. Malformed/absent env ⇒ attestation off (escape hatch). Parity between the two implementations is pinned by `attestutils_test.go`'s mirrored-walk tests; packager env-stamping covered by `internal/adapters/packager/attestation_test.go`; supervisor verify covered by `attest_test.go`. Only layered attests; exe/static are unaffected. **Option A (stub launcher) still remains.**
  - Recommended composition: **A + C** together (they compose independently; either alone is a partial mitigation).
- **Flags/Interface**: none proposed — default hardening, matching how `readOnlyRootFilesystem` and Readiness-Drain-on-SIGTERM shipped with no new flag.

### 3. Dedicated `chainguard-static` Base Image Preset (Low-Medium Priority)
- **Problem**: `--strategy=static`'s default base (`cgr.dev/chainguard/static:latest`) reuses the existing `BaseImageChainguard` preset to get correct signature verification (proven safe — this codebase's own doc comment cross-validates the Fulcio SAN is identical across Chainguard images). That leaves a narrow residual `pokkum.lock` collision: an explicit `--base cgr.dev/chainguard/glibc-dynamic` (layered) build and a `--static` build in the same project now share one lock slot for two non-interchangeable base images.
- **Solution**: add a dedicated `BaseImageChainguardStatic` preset (own default ref, own `pokkum.lock` key, same proven-correct Chainguard signing identity) instead of reusing `BaseImageChainguard`. Full design, including a backward-compatibility analysis for existing `pokkum.lock` files with an orphaned `"chainguard"` entry, in `concepts/new-chainguard-static-preset-concept.md`.
- **Flags/Interface**: none — internal preset wiring only; `--static`'s existing default-base behavior is unchanged from the user's perspective.
- **Note**: this touches signature verification (a supply-chain-correctness concern), which is why it's prioritized here rather than deferred like the Strategy-Dispatch Consolidation item in "Beyond v1.0 / Long-term Backlog" below, despite both being "nice to have, not urgent" — see the concept doc's own open question on this trade-off.

### 4. Policy as Code Enforcement (Low Priority / Backlog)
- **Problem**: Security compliance rules (e.g., "no critical CVEs", "must contain SBOM", "must run as non-root") are currently validated imperatively across disparate flags.
- **Solution**: Integrate Open Policy Agent (OPA) / Rego policy evaluation to assert organizational supply-chain and container security policies against image metadata, SBOMs, and scan results prior to publishing.
- **Flags/Interface**: `pokkum policy check --policy=<path>` subcommand and `--policy=<path>` build gate.

---

## Beyond v1.0 / Long-term Backlog

_Features planned for future architectural iterations:_

- [ ] Multi-Environment Management: Config templating and secret-manager integrations. (new flag: `--target-env=<name>` on `build`/`resolve`/`apply`)
- [ ] Hooks System: Pre/post-build hooks (shell, Bun scripts, webhooks). (new subcommands: `pokkum hook pre-build`, `pokkum hook post-build`; new flag: `--skip-hooks` on `build`)
- [ ] Service Mesh Integration: Auto-generate Istio/Linkerd configs and mTLS paths. (new subcommand: `pokkum mesh generate`, new flags: `--mtls`, `--mesh-telemetry`)
- [ ] Progressive Deployment Strategies: Canary/blue-green deployments for advanced GitOps controllers. (new subcommand: `pokkum deploy`, new flags: `--canary=<percent>`, `--blue-green`, `--auto-rollback`)
- [ ] Asset Optimization Pipeline: Automatic WebP/AVIF variants, cache-busting, CDN-origin separation. (new flag: `--optimize-assets` on `build`)
- [ ] Plugin System: Extensible via external packages (high complexity/supply-chain risk). (new subcommands: `pokkum plugin add|list|remove <name>`)
- [ ] `--verify-base-on-cache-hit` (opt-in, strict): On a confirmed remote-cache hit, still run `BaseImageResolver.VerifyBaseImage` before accepting the hit. Today a cache hit deliberately skips base-image signature verification (nothing is built from the base on a hit, and the cache key already binds the base image digest via `pokkum.lock`); the skip is disclosed with an explicit audit log line. This flag exists for strict supply-chain-audit environments that want the uniform guarantee "every emitted/promoted image traces to a verified base, hit or miss" — notably catching the narrow case of a base whose pinned digest still matches but whose signature was later revoked/rekeyed. Must stay **structurally independent** of `--cache-verify` (which authenticates the cache-hit *output* image; this flag authenticates the *base*). Opt-in so the sub-100ms fast path is preserved by default. (new flag: `--verify-base-on-cache-hit` on `build`; see `AdditionalFeatures.md`)
- [ ] Strategy-Dispatch Consolidation (see `concepts/strategy-dispatch-refactor-concept.md`): `ports.BuildStrategy` dispatch (`layered`/`exe`/`static`) is scattered across ~20 sites in 4 files with no compiler-enforced completeness — a future 4th strategy risks a missed branch. The concept doc proposes 3 package-local dispatch tables (not one cross-cutting interface — that would violate the hexagonal boundary between `internal/core` and the adapters). **Explicitly deferred by the concept doc's own recommendation**: do not build until a concrete 4th strategy, or a second real bug caused by the scattered pattern, makes the maintenance cost real rather than projected.

