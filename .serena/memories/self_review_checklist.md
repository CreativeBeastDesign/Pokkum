# Self-Review Checklist

A concise, invariant-style checklist run against every non-trivial code diff before declaring it complete, as part of the verification protocol in `mem:task_completion`. Unlike `Lessons.md` (an append-only, chronological incident log kept in the repo root), this memory holds only the *currently-active, distilled* rules and is edited in place, not appended to — that's why it lives in Serena rather than the log itself.

Shared across every agent/harness working this codebase (each has Serena access) — this is the one source of truth for the checklist; do not fork a duplicate copy into a harness-specific instruction file.

## How to use

1. After a diff is otherwise ready (tests green, `make verify` passing per `mem:task_completion`), walk every row below against the actual diff lines — not from memory of having read it once.
2. If a row doesn't apply, say so explicitly ("N/A: no concurrent dispatch in this diff") rather than skipping it silently.
3. If a bug is found during this pass, log the root cause and preventative rule in `Lessons.md` (repo root), then come back and close the loop here (see Maintenance rule below).

## Checklist

| # | Check | How to verify |
|---|-------|----------------|
| 1 | Fan-out error paths — prefer structurally impossible over carefully avoided | For every `errgroup.Group`, `sync.WaitGroup`, or bare `go func()` touched by the diff: grep for every `return`/`continue`/`break` sitting between the dispatch call and its corresponding `Wait()`/`Done()`. Each one is a leak candidate until you can point to why it's safe. **When you find or fix one, prefer a structural fix over a guard**: if the fallible decisions that can error (validation, skip/reuse checks) can be fully separated from the dispatch loop — computed in an earlier pass with zero goroutines in flight, so no fallible call is ever positioned between a dispatch and its `Wait()` — do that separation instead of just adding a careful early-return. A guard only holds as long as every future edit happens to preserve an invariant nothing in the code enforces; this exact bug recurred in `internal/adapters/k8s/resolver.go`'s fan-out (see Origin note) after being fixed once with a guard, and was only fixed for good once the loop was restructured into a validate-then-dispatch two-pass shape that makes the leak unreachable by construction. |
| 2 | Resource cleanup on every branch | For every `os.File`, `io.Reader`/`io.Closer`, network conn, or lock acquired in the diff: trace every return path, including new early-return branches just added, and confirm cleanup runs on each — not just the happy path. |
| 3 | Multi-item collection logic | If the diff processes a collection (map, slice, multi-document stream), does at least one test use ≥2 items with *differing* content/outcome? Single-item or all-identical fixtures hide item-interaction bugs. |
| 4 | Non-first-item failure injection | For every new error path inside a loop or fan-out, does a test trigger the error on a *non-first* item, not just the first/only one? A single-item error test can pass while a real multi-item failure leaks resources. **This row and row 1 have already failed to prevent a recurrence once** (see Origin note) — a guard-shaped fix passed this exact check at the time it was written, then regressed when a later edit reintroduced interleaved dispatch-and-validate logic that nothing forced a re-check against. Treat a passing test here as necessary, not sufficient: also ask whether the fix could be made structural (row 1) so the next edit can't silently reintroduce the interleaving in the first place. |
| 5 | Ordering / determinism | Are map iterations, directory traversals, and concurrently-produced results consumed in an explicitly sorted or otherwise deterministic order (bit-for-bit reproducibility invariant, `mem:core`)? |
| 6 | Clock / timestamp access | Does the diff introduce any `time.Now()` or system clock read outside `req.SourceDateEpoch`? |
| 7 | Go version regression | Does the diff touch `go.mod`'s `go` directive or any `go-version:` line in `.github/workflows/*.yml`? If so, `git diff` those exact lines and confirm the version only holds steady or increases — never decreases. |
| 9 | Runtime-vs-construction validation | Does the diff rely on a precondition that can change at runtime (a file's existence/type, openness) but only validate it once at construction/setup? If so, the code path must re-check the precondition at the point of use and degrade to the normal-miss path, never escalate to 5xx or serve stale state. |
| 8 | golangci-lint clean | Has `golangci-lint run ./...` (or `make lint`, folded into `make verify` as of the step below) actually been run against this diff — not just `go vet`? `gofmt`/`go vet`/`go test` all pass on findings golangci-lint alone catches (`errcheck`, `staticcheck`, ...); see the Origin note below. |
| 10 | Base-vs-profile config field parity — validation AND merge | If the diff adds, removes, or changes a `ports.ImageConfig`/`ports.ProjectConfig` field: (a) does `cmd/pokkum/config.go`'s `runConfigValidate` (or the shared `validateConfigFields` helper) apply the same check to it via the per-profile iteration over `cfg.Profiles`, not just the base-config call? (b) separately — a distinct function, a distinct miss — does `internal/adapters/config/config.go`'s `ApplyProfile` include an explicit `if profile.Image.X != zero { merged.Image.X = profile.Image.X }` line for it? `ApplyProfile` is hand-written per-field, not reflection-based, so a new struct field is silently invisible to `--profile <name>` (parses fine, validates fine, then discarded) unless a matching merge line is added — this is a *second*, independent place the same new field can be forgotten, not covered by fixing (a) alone. Grep both `runConfigValidate` and `ApplyProfile` for the new field name; both must appear, or say explicitly which one legitimately doesn't apply and why. |
| 11 | Strategy-gated logic scoped to the right strategy | For any `if`/`switch`/`else` in `internal/adapters/bunexec`, `internal/adapters/packager`, or `internal/core/pipeline.go` that branches on `req.Strategy`/`ports.BuildStrategy`: is an `else`/`default` arm actually correct for *every* strategy it covers, or was it written assuming only one specific strategy would ever reach it? A negative check (`!= StrategyX`, "not static") is not the same as a positive check (`== StrategyY`) — the former silently includes every future or existing strategy nobody thought about when the branch was written. Trace which strategies actually execute the branch and confirm each one's real toolchain output matches what the branch assumes. |
| 12 | Fixture fidelity to the real consumed artifact — and its provenance claim | For a fixture/mock claiming to be "real" (sourced from an actual upstream package, a real tool run, a real npm package, etc.): is it sourced from the *exact pipeline stage* the code under test actually reads — or from an earlier stage (e.g. a package's source template) that merely resembles the final artifact? A fake-bun/fake-tool test harness's fixture content must model what the real tool would emit for that exact code path, not just whatever satisfies the current implementation — check this by asking "would running the real tool right now actually produce this?", not "does this fixture make the test pass?". A doc comment asserting "captured verbatim from a real run of X" is itself a claim to verify, not evidence on its own — especially for a sub-agent-authored fixture — so if the real command is runnable in this environment, actually re-run it and diff the output before trusting the comment. **This also covers a test double's capability configuration, not just its content**: for a test claiming to exercise a capability-gated code path (an opt-in registry/API feature, a feature flag, a protocol version), check that the mock/fake/in-process server actually has that capability turned on — a double that defaults to the less-capable configuration can make an "advanced path works" test pass by accident via whatever fallback exists for the less-capable case, silently turning a positive test into a no-op (see `Lessons.md`'s 2026-08-17 `--sbom-attach=referrer` entry, where the in-process test registry's OCI 1.1 referrers support was off by default). **This recurred 2026-08-19** in a brand-new package (`internal/adapters/bunexec`'s static-strategy prerendered handling): a synthetic `staticFixtureCompiler` fabricated a flat `prerendered/index.html` tree — the same wrong assumption the production flattening code made — instead of the real nested `prerendered/pages/` shape `@sveltejs/adapter-static` actually emits, so the one test exercising it agreed with the bug rather than catching it. This row's coverage is not a one-time closed item per package; re-apply it whenever a new adapter/strategy integration gains its own synthetic fixture, not just the ones already named in this Origin section. |
| 13 | Caller-chain coverage for a same-diff fix | If the diff fixes function B, and B is called by A before some other check in A runs (or by C after B), does at least one test exercise A (or C) end-to-end with the same real-world input the fix targets — not just B in isolation? A thoroughly unit-tested fix can still leave the real call chain broken if a sibling check earlier or later in the same chain was never revisited. Grep the fixed function's callers; if any of them do their own independent validation before/after calling it, that caller needs its own test with the fix's target input, not just the fixed function. **This recurred 2026-08-19**: `internal/adapters/bunexec/compiler.go`'s `Preflight` — called by `core.Build` ahead of `Prepare` in the exact same real call chain this row was written about — hard-required a bun/node adapter and had no `Strategy` field to consult, rejecting every correctly-configured `adapter-static` project before `Prepare`'s own strategy-aware check ran. The row already named this function pairing from a prior incident and did not by itself prevent the second occurrence, because nothing forced re-running its discipline when a new `ports.BuildStrategy` value was added. Treat this row as needing re-application on every new strategy/enum value added to a dispatch a sibling function in the same chain also branches on, not as permanently satisfied once the named functions were fixed. |
| 14 | Auto-fix/auto-inject features need a fixture the feature is meant to fix | For a diff touching a feature whose entire point is to correct, inject, or paper over a misconfiguration (zero-config injection, auto-repair, `--fix` flags, defaulting logic): does at least one test fixture start in the *broken* state the feature exists to handle, not only already-correct fixtures? A fixture that's already correct can pass every assertion whether or not the feature under test does anything at all — it structurally cannot tell you the feature works. |
| 15 | External compiler/tool output determinism, and unverified non-determinism hypotheses | For any diff invoking an external compiler/tool (`exec.Command`) whose output becomes a shipped artifact (embedded binary, layer content): if any argument or `cmd.Dir` is an absolute path that varies per invocation (an `os.MkdirTemp` directory, PID, timestamp), does the artifact's byte-identity depend on that path? Verify empirically — run the exact production invocation shape twice with only that one input varied and diff the output — rather than shipping a fix based on a plausible-sounding but unverified mechanism (linker build-id, ASLR, embedded timestamps). A repro that changes two things at once (e.g. both the output filename and the source path) can implicate the wrong mechanism entirely; isolate one variable at a time before designing the fix. |
| 16 | Feature reality check before marking an item `status: shipped`, and before *starting* a fix for a roadmap item's problem statement; test-shape vs. test-substance | Before setting an item's `status: shipped` in `docs/roadmap/<area>.yaml` — and before trusting an existing status that already looks satisfied — grep the implementing file for the I/O primitive the feature's description necessarily requires: a command claiming to inspect a remote image must reference `remote.`/`Fetch`/a tar reader somewhere; a claimed endpoint must register a handler for that exact path; a claimed emitted field must contain that field's literal name in the producing code. Absence of the required primitive is proof of absence of the feature, full stop — a passing test is not counter-evidence if the test only asserts output *shape* (correct JSON keys, correct exit code) rather than asserting the output was *derived from real input* (e.g. a digest that changes when the underlying image changes). For a feature named with "X and Y" (two halves in one description, one flag, or one row): verify each half independently: it is common for one half to be real and the other a stub, and a test exercising only the real half will pass cleanly while the row still overclaims. **The mirror case, at the start rather than the end of a fix:** an *open* roadmap item's `problem` field is also an unverified claim — if it says "`--some-flag` triggers X," grep `cmd/pokkum/*.go`'s actual flag registrations for that exact flag name before starting. A Go struct field or port request parameter existing is not evidence a CLI flag reaches it; the field can be real and correctly consumed while the flag that's supposed to set it was never wired at all, making the "fix" invisible from the CLI until that's caught too (see `Lessons.md`'s 2026-08-17 `--bun-version` entry). **Same discipline applies to a documented invocation path, not just a documented feature or flag**: when rewriting or verifying a command's docs, run `--help` (or `go run ./cmd/pokkum <claimed-path> --help`) against the actual built binary and diff it against what the docs claim the command line looks like — a subcommand nested under a different parent than the docs describe (`pokkum explain why` documented as top-level `pokkum why`) is the same class of unverified claim as a fabricated field, just cheaper to check (see `Lessons.md`'s 2026-08-17 `pokkum why`/`pokkum diff` entry). **Also check field-level provenance within an otherwise-real feature**: for each field in a new "reads real data" command's output, does it come from the *resolved/verified* result, or does it just echo an *input parameter* (a flag, a request field) that only some of the function's code paths actually use to produce that result? An echoed-but-unverified input looks identical to a real observation in every unit test that happens to pass a value already consistent with the fixture — it only diverges against a real input crafted to disagree, which is exactly why a manual smoke test against a real build (not just synthetic fixtures) caught this once already (see `Lessons.md`'s 2026-08-17 `pokkum explain`'s `Platform` field entry). |
| 16b | A dependency required-and-validated as non-nil is not evidence it is ever called | For any diff wiring in a signer/verifier/other side-effecting dependency that `Validate()` or an equivalent constructor-time check requires to be present: grep the actual execution path (not the validation path) for a call to its real method (`.Sign(`, `.Verify(`, `.Attach(`, etc.). A dependency can be required, checked for nilness, and threaded through every constructor while the one line that actually invokes it is missing entirely — this reads identical from the outside (no error, clean build) to a working implementation, and is indistinguishable from success in every test that only asserts the dependency was *constructed*, not *invoked*. This is a specific, sharper instance of row 16's "grep for the required I/O primitive" discipline — added as its own row after the exact failure recurred on the exact category row 16 was written to catch (see `Lessons.md`'s 2026-08-18 image-signing entry: `--sign` defaulted true, `CosignSigner`/`DSSESigner` were required non-nil, and neither's `Sign()` was ever called anywhere in the pipeline). |
| 24 | A security check's expected value must originate outside the artifact being checked | For every comparison inside a verification/signature/attestation path (`==`, `strings.Contains`, a certificate field read, a claimed-identity lookup): trace where the value being compared *against* (the expectation) came from. If it was read off the same certificate, signature, payload, or artifact the check exists to validate — rather than from a flag, a config file, or an operator-chosen default tied to a known publisher — the check is tautological: it will pass for a genuine artifact and an adversarially crafted one identically, because the artifact is grading its own homework. This is easy to miss because the code often *looks* like a real check (a real field is read, a real comparison runs) and a test asserting only `valid == true`/`valid == false` on today's fixture cannot distinguish a self-referential check from a real one — both return the same boolean on the one fixture available. When fixing such a check, audit the fix itself for the same defect one level up: does comparing the corrected field against *itself* rather than an external expectation make the check trivially satisfiable by anything the artifact carries? (See `Lessons.md`'s 2026-08-18 keyless-identity entry: the naive fix — reading the real OIDC issuer extension and comparing it to itself — would have made any Fulcio-issued certificate on earth verify, which is worse than the dead-code bug it replaced.) A dead security check and a universally-passing one are indistinguishable from the outside and from most test suites; both require tracing every comparison's operands back to their source, not just confirming a comparison exists. |
| 25 | A control whose absence is indistinguishable from success will eventually be absent | If a diff adds a security control (a signature, an attestation, an integrity check) whose entire job is to make some later consumer trust an artifact: does anything verify, immediately and within the same operation, that the control was actually applied and is actually retrievable/checkable by that later consumer — or does "the step ran with no error" stand in for "the control now genuinely protects this artifact"? A control that can silently no-op (a signer that's never called, a check that's skipped on one branch, a flag that's parsed but never read) looks byte-for-byte identical to one that worked, in every log line and every green test, until a real consumer downstream tries to rely on it and fails — often much later, and far from the code that dropped it. The fix is architectural, not just a test: have the producing step read back what it just wrote from the real destination (the registry, the filesystem, wherever the consumer will actually look) and cryptographically/structurally re-verify it before reporting success, so "reported signed" becomes impossible to reach by any path that didn't genuinely produce a fetchable, verifiable artifact. See `Lessons.md`'s 2026-08-18 image-signing entry: `core.signAndSelfVerify` now fetches the just-attached signature and attestation back from the registry and re-verifies them cryptographically before the build is allowed to report `Signed: true` — the same discipline this row asks for, applied as the actual fix, not just the review comment. Distinct from row 25's sibling, row 16b: 16b is about *detecting* a dependency that's never called; this row is about *designing the control so its own absence becomes loud* rather than relying on review to keep catching it. |
| 26 | Text-scanning bounded by a fixed token/line/size constant needs a repro against realistic *generated* input, and "scan step errored" must be distinguished from "scan step found nothing" | For any diff that scans file content line-by-line or in fixed chunks (`bufio.Scanner`, a fixed-size read buffer, a regex applied per-line): what is the actual size/length ceiling, and is there a test that constructs input at or beyond it using *realistic generated content* — a real minified/bundled file, not hand-written fixtures, which structurally cannot produce a pathological single-line-is-the-whole-file shape? `bufio.Scanner`'s default 64KB token limit is the single most common trap in Go, and it fails silently unless explicitly checked: it returns `bufio.ErrTooLong` from `Scan()`, which a caller that treats "any error from the scan step" as "skip this file / report clean" converts into a false-negative that looks identical to genuinely-scanned-and-clean. Separately, check whether a regex/pattern match loop stops after the first match per line/file (`FindStringIndex`+`break`) rather than finding all matches (`FindAllStringIndex`) — on realistic minified content where an entire file can be one line, this hides every finding after the first regardless of the size-limit bug. Both failure modes must surface as a distinct, typed result ("could not scan this file," e.g. a skip list plus a dedicated sentinel error) rather than being silently folded into "nothing found here" — the two are not the same claim, and a security-relevant scanner must never conflate them. See `Lessons.md`'s 2026-08-18 secretguard entry. |
| 17 | Packaged output must actually execute, not just exist with the right bytes | For any change to what gets packaged into a container layer (`internal/adapters/packager/*`, `internal/core/pipeline.go`'s `pkgReq.*Dir` assignments, anything touching `AppServerDir`/`AppClientDir`/etc.): extract the actual built layer from a real end-to-end build (a real fixture, not a synthetic mock) and run its entrypoint (or the specific artifact) directly — `tar -xzf` the layer blob, `cd` into it, execute it, confirm it actually starts/serves. Tar-member presence, byte-for-byte determinism, and digest stability tests (which this codebase has many of) prove none of this — a file can be present, byte-correct, and still fail to run once extracted into its real sibling directory structure (e.g. a relative import that assumed a different nesting). If the only test coverage for a packaging change is a synthetic mock's directory shape, confirm that mock's shape was itself checked against a real build of the real upstream tool (adapter-node, Bun, etc.) — a mock can encode the same wrong assumption as the bug it's meant to catch. |

| 18 | Security control scope — every untrusted-execution site, not just the one named | When adding a security control that wraps subprocess execution (a sandbox, a network restriction, a privilege drop): grep the whole package for every `exec.Command`/`exec.CommandContext` call, not just the one a roadmap item's line reference or bug description points at, and ask "does the same untrusted-code-execution risk this control is closing also apply here" for each one. A multi-stage pipeline is exactly where a partial fix hides — the fixed stage's tests pass cleanly, giving a false sense of completeness while a later stage runs the same untrusted input with zero protection. Verify the control actually blocks the thing it claims to, empirically (a real listener/real probe the sandboxed process is proven unable to reach), for every site, not just one representative site. |

| 19 | Cross-process test state — an in-process variable override does not reach a separately-compiled subprocess | If a test needs to exercise code that runs as a genuinely different OS process (a `/proc/self/exe` reexec helper, a hidden CLI subcommand, anything invoked via `os.Executable()` rather than called as a plain function) — check that whatever the test overrides to control that code's behavior actually crosses the process boundary. Overriding an in-process package variable only affects the *test binary's own* compiled copy of it; a separately-built binary (even from the identical source) has its own independent copy, unaffected by the test's mutation. Only environment variables, files, sockets, or other genuinely cross-process channels reach the other process. If the test passed anyway, ask why — a false pass here looks identical to a real one (no error, just the wrong path silently taken), which is what makes it dangerous. |

| 20 | Extract-then-repackage path round-trip fidelity | For any diff that extracts content from a packaged location (a tar entry under some prefix) into a scratch directory later re-packaged under a prefix (same or different) — not a plain file copy, an extract → intermediate-directory → repackage round-trip: does at least one test assert the *exact* relative path written to the intermediate directory, and ideally the *final, fully-reconstructed* in-image/consumer-facing path — not just "content landed somewhere under the scratch dir with the right bytes"? A test that only checks reachable-content-with-right-bytes at an unasserted sub-path can pass even when a prefix-stripping bug silently drops or adds a path segment, because the wrong sub-path still satisfies "some file, correct bytes, under the directory." Also check that the fixture actually models the real nested directory shape the feature operates on (e.g. `_app/immutable/chunks/...`, not a flattened single-level filename) — a flattened fixture used for unrelated convenience (a test helper that doesn't `MkdirAll` nested paths) structurally cannot expose this bug regardless of how carefully digests are checked. |

| 21 | Cross-layer record aggregation must dedupe by final path, matching real layer-squash semantics | If a diff can produce TWO OCI layers that legitimately target the same in-image path/prefix (e.g. two separate `mutate.Addendum`s both rooted at the same directory), and their per-file records feed one aggregate digest/manifest/index computed by concatenating without dedup (grep for `RootDigest`, or any `append(records, moreRecords...)` pattern feeding a hash): does at least one test use a fixture where BOTH layers contain the SAME relative path (with different bytes, so a wrong answer is detectable), and assert the aggregate matches an oracle that also dedupes — modeling the real, single, layer-squashed filesystem a container actually sees, not a bag of per-layer records? A test whose fixture only uses non-overlapping paths across the two layers cannot expose double-counting, and an oracle that mirrors the same non-dedup logic as the code under test will agree with a bug instead of catching it. Also verify addenda/record ordering: OCI layers apply in append order, so `dedupe(..., keep=last)` must match the SAME order records were accumulated in — if the layer whose bytes are semantically "supposed to win" (e.g. the current build, not a stale carried-forward asset) isn't appended last, verify a colliding path is either (a) provably impossible by construction, or (b) explicitly reordered so it wins. |
| 22 | Untrusted-source path-write containment: clean BEFORE prefix-check, not after, plus a second independent containment check at the actual write | For any code that reads a path string from content sourced from something other than the local, already-trusted filesystem (a tar entry from a pulled registry layer, an uploaded archive, any externally-supplied path) and uses it to construct a destination write path under some root directory: is the path cleaned (`path.Clean` for POSIX-style sources like tar entries, `filepath.Clean` for host paths) BEFORE any prefix/allowlist check runs, not after? A raw string can satisfy a `strings.HasPrefix` check while still containing an unresolved `../` that only gets resolved later by `filepath.Join`'s own internal `Clean` — by which point the entry has already been accepted. Separately, and even when the clean-before-check order is right: is there a SECOND, independent check at the actual `os.WriteFile`/`os.Create` call site verifying the final joined path still lies under the destination root (e.g. via `filepath.Rel` or a `destDir+separator` prefix check on the cleaned absolute paths)? Belt-and-suspenders is required here, not optional — the first check is only as strong as every code path that reaches it staying correct, and a future refactor that adds a second call site to the same write logic could easily bypass an early-only check. A regression test for this must prove containment empirically (assert nothing was written outside a real temp directory with a crafted `"../"`-laden entry), not just assert an error was returned — an error from the WRONG cause can still pass a test that only checks `err != nil`. |
| 23 | A resolved identifier (digest, hash, ID) that crosses a call boundary where different callers/entries may legitimately belong to different scopes must carry its scope with it, not rely on a single default | If a function resolves some identifier (a content digest, a hash, a database ID) and returns just the bare identifier to a caller that then uses it against a DEFAULT scope (a default repository, a default namespace, a default tenant) — check whether every possible caller of that resolve function is actually guaranteed to belong to that same default scope. A port/flag/parameter that is documented or designed to accept cross-scope input (e.g. `--asset-overlay-from` explicitly naming "arbitrary external images, not this build's own push target") breaks that assumption silently: the resolve step succeeds (it has no reason to know or care what scope the caller will later assume), and the failure only surfaces later, at the point the bare identifier is used against the wrong default scope — typically a confusing not-found error far from the actual bug. The fix is to have the resolve function return a fully-scoped identifier (e.g. `repo@digest` instead of a bare digest) whenever the input could plausibly cross scopes, and have the consuming function distinguish "already fully-scoped" from "bare, use my default" (e.g. by checking for a scope-separator character your bare form can never contain) rather than unconditionally prepending its own default. A regression test must actually construct two different scopes (two different repos/namespaces) and prove an identifier resolved in one is correctly used against that same one, not silently coerced into the caller's default. |

| 27 | An assertion that pins current (possibly buggy) behaviour is not a test | For any test asserting an exact value derived from what the code *currently* produces (a timestamp equals whatever env var the code happens to read, a count equals whatever the current loop happens to emit) rather than from an independently-reasoned expected value: ask whether the assertion is checking a specification or merely transcribing production behavior. A transcribing assertion looks identical to real coverage right up until the transcribed behavior turns out to be the bug — at which point the test actively resists the fix (the correct change makes a "passing" test fail) instead of protecting against regressions. When fixing a bug, always re-read every assertion that exercises the buggy code path and confirm each one's *expected* value was independently reasoned about, not copied from the bug's own output. |
| 28 | A more-permissive cache key can make a determinism/stability test vacuous | If a diff changes a cache/memoization key to be more permissive (e.g. content-only instead of content+timestamp, so more calls now count as "the same"), any test that builds/computes the same logical input twice and asserts something is stable *across* those two calls must confirm each call actually re-executed the code path under test — not that a shared cache (an on-disk cache dir, a package-level memo, a test-process-wide singleton) silently returned the first call's result for the second. Isolate the cache (a fresh temp `POKKUM_CACHE_DIR`-equivalent per test invocation) whenever the cache's own new hit criteria could otherwise collapse two intentionally-varied invocations into one — a cache hit and a genuine recomputation are indistinguishable from the assertion's point of view, so the test cannot tell you which one happened unless you rule the cache out by construction. |
| 29 | Register cleanup at the point of allocation, not at the point of confirmed success | For any test or code cleaning up an external resource (a container, a file, a lock, a subprocess) after a call that is not atomic — i.e. the external system *allocates/registers* the resource as an earlier, separate step from *starting/using* it (e.g. `docker run -d --name X` registers the named container before attempting to start it) — is cleanup registered immediately after the allocating call, unconditionally, rather than only after confirming the call fully succeeded? A call that fails partway through can still have allocated the resource, and "cleanup runs only on success" leaks exactly the resources created by the failure path a test is most likely to exercise. Check the external tool's actual allocate-vs-start semantics before assuming a failed call created nothing. |
| 30 | Prove "fails closed" by reintroducing the removed fallback and watching the test break | For any diff that removes a fallback/default which allowed some path to silently proceed (a placeholder trust anchor, a default value standing in for "not configured"), and adds fail-closed behavior in its place: does the regression test actually distinguish "checked and failed" from "nothing was there to check"? A verification result reported as `valid bool, err error` (or any two-state shape) can have a state — "there was something to verify and nothing configured to verify it against" — that collapses onto the same `false`/`nil`-adjacent values as an ordinary negative result, making a fail-open indistinguishable from a working check in any test that only asserts the boolean. Prove the fix by temporarily reintroducing the deleted fallback (or reverting the fix) and confirming the new test actually fails — not just that it currently passes. When auditing multiple parallel sites for the same defect (e.g. three independent trust-anchor fallbacks being removed together), apply this proof to every site individually; a fail-open at one site can hide behind superficially similar code at siblings that are genuinely fixed. |
| 31 | A build-input knob — including one the toolchain adds automatically — must not leak into an artifact whose content doesn't depend on it, and a reproducibility guard must vary the dimension that actually changes in production | For any diff threading a build-time knob (a timestamp like `SOURCE_DATE_EPOCH`, a flag, an env var) uniformly into every layer/artifact a pipeline produces: check whether each artifact's actual *content* depends on that knob. A knob that correctly varies some artifacts (source-derived content, keyed to the commit/run that produced it) can still be wrong to thread into a sibling artifact whose bytes are independent of it (a pinned third-party binary, a version-pinned toolchain component, anything cached and reused across runs) — uniform plumbing is convenient to write but not automatically correct, and threading a per-run-varying value into a per-content-invariant artifact's cache key/timestamp defeats caching and cross-fleet deduplication for that artifact on every single run. Trace each artifact back to what actually determines its bytes before deciding whether a knob belongs in its construction. **This class also covers metadata the toolchain stamps in by default, without the diff ever explicitly threading it** — Go's `-buildvcs` embedding `vcs.revision`/`vcs.time`/`vcs.modified` into every binary is the concrete recurrence (2026-08-19, the second instance in these exact layers after `SOURCE_DATE_EPOCH`): nobody wrote code that passes a knob in, the compiler default did it unasked, and the artifact was still not content-invariant as required. **And when the fix is a reproducibility/determinism guard itself, verify the guard exercises the axis that actually varies in real use** — building the same source twice at the same commit and the same dirty/clean state proves only within-invocation stability, which passes unconditionally and is not the property that matters; the property that matters here is stability *across commits* (or clean vs. dirty tree), and a guard must actually vary that dimension (two different commits, or one clean and one dirty checkout) to be evidence of anything. |

| 32 | Deleting a default/fallback requires grepping every `nil` test on the field it fed, not just the removal site | For every removed default/fallback in a dependency-injection chain (a constructor default, a placeholder trust anchor, an env-var fallback): grep the whole package — not just the one call site the removal targets — for every `== nil`/`!= nil` test on the field that default used to guarantee non-nil. A condition that reads as an ordinary defensive guard while the default made its nil branch unreachable becomes a fail-open the instant that default is gone: it will silently *skip* the check it guards rather than *refuse*, and report a passing-shaped result (`false, nil` or equivalent) indistinguishable from a genuine pass. This is the search-phase companion to row 30 (which proves the fix by reintroducing the fallback and watching a test fail) — row 32 is about finding every site that needs that proof in the first place, not just the one the change description names. |
| 33 | An embedded or generated artifact consumed by production code must be built by the pipeline that ships it | For any `go:embed` directive, vendored binary, or other checked-in-adjacent generated artifact: grep for the make target/script that produces it, then confirm something in CI *and* in the release pipeline (`.github/workflows/*.yml`, `.goreleaser.yaml`) actually invokes that target. A compiler happily embeds an empty, stale, or gitignored-and-therefore-absent directory with no error — absence of a build failure is not evidence the artifact was actually produced fresh, or produced at all. For a supply-chain/attestation tool specifically, also ask whether the component enforcing the attestation is itself covered by it — a PID-1 binary built on a developer laptop while the CLI's own SLSA provenance claims a hermetic CI build is the sharpest version of this gap. |
| 34 | An embedded trust anchor or pinned security snapshot needs a machine-readable capture date, an always-on staleness test, and a diagnosis that distinguishes coverage gaps from bad artifacts | For any embedded/pinned security material with a real-world expiry or rotation cadence (a trust root, a certificate bundle, a revocation list): is there (a) a recorded, machine-readable capture date alongside the bytes; (b) a test that *fails* (not warns) once age exceeds a threshold deliberately much shorter than the material's actual validity window, so drift is caught long before it matters; and (c) an error message that tells the operator "our anchor doesn't cover this" rather than implying "this artifact is bad" when a lookup fails for coverage reasons? Collapsing (c) into a single generic failure trains operators to distrust a working control or bypass it outright — the two failure modes demand opposite responses. Also verify a third-party module's own bundled "example"/"reference" copy of the same material before treating it as a refresh source — it is not guaranteed to track its dependency's live state and may itself be the stale artifact. |
| 35 | A real-build test's fixture directory is not read-only by convention — check what actually writes into it, across the whole package and any sibling package sharing the same `testdata/fixtures/*` path | For any test that threads a `testdata/fixtures/*` path into `core.Build`, `Compiler.Prepare`/`Compile`, `Packager.Build`, or any `ports.Compiler`/`ports.BaseImageResolver` double as `ProjectDir`: does anything in that call's real dependency chain (a mock `Prepare` that writes fixture files, the real `bunexec.Compiler` running `bun` with `cwd=ProjectDir`, a real `BaseImageResolver` writing `pokkum.lock`) actually call `os.WriteFile`/`os.MkdirAll`/`os.RemoveAll` against it — checked by reading the actual production code, not inferred from "the test passes"? If yes, that test must copy the fixture into `t.TempDir()` first (symlinking `node_modules` rather than copying it, to keep large dependency trees cheap) and use the copy as `ProjectDir`, never the checked-out fixture path directly — even if today's failure mode is only "leaves scratch behind," a fixture two tests/packages both build against directly is one flaky rerun away from an order-dependent failure. A doc comment that names the hazard ("the fixture may be shared with other tooling") without eliminating it is not a fix — grep the rest of the package, and any other package importing the same fixture by path, for the identical unguarded pattern before considering the row closed. |
| 36 | A third-party consumer's interop contract must be verified against that consumer's own source or spec, not inferred from its error message | For any diff producing an artifact a named external tool is expected to read (an OCI annotation, a manifest shape, a file format another vendor's CLI parses): if a comment or design assumption states what that external tool requires or tolerates, trace it to the tool's actual source or published spec before shipping — not to the error string it happens to produce today, which can describe the wrong cause entirely (a missing *key* vs. an empty *value*, a type mismatch vs. a missing field). This is the mirror of row 16: row 16 asks "does my code actually do what I claim about itself"; this row asks "does what I hand to someone else's code satisfy *their* code's real requirement, not my guess at it." Prove it by running the real external tool against a real produced artifact, both before and after the fix — a unit test asserting only "the annotation is present" cannot distinguish a present-but-wrong value from a correct one. |
| 37 | Executing a piece of documentation is itself a test, and finds bugs that reading it cannot | For any guide, runbook, or README section that gives the reader literal commands to run against the real system (not prose describing behavior): before trusting it as accurate, actually run every command against the real, current binary/API — a step's own internal consistency ("this looks right, and the next step follows from it") is not evidence it matches what the code does today. A document can be entirely self-consistent and entirely wrong, because reading it for correctness only re-derives the author's own mental model, which is exactly what produced the error in the first place. When a documented procedure fails on live execution, root-cause the *documented claim* itself rather than just patching an errata line — the same discipline row 16 already asks for the codebase's own claims extends to any document making claims about the codebase. |
| 38 | A cache/lock key must be as granular as the space of distinct values that can legitimately share it | For any on-disk or in-memory cache/lockfile keyed by a *category* (a preset name, a mode string, a strategy enum) rather than by the *specific identity* of what's being cached (a full reference, a content hash, a resolved digest): check whether two logically distinct inputs can produce the same category value. If so, a second request under the same category key does not merely evict or miss the first entry — it can be silently served the *first entry's content* under the second request's name, which is wrong output, not a performance regression, and nothing signals a miss: the lookup succeeds, and the wrong data satisfies every consumer that only checks "was something found." When the coarse key also carries authoritative metadata (a pinned digest, a scan result), require the cached entry's own recorded identity (its ref, its digest) to match the current request before trusting it — a narrow guard that needs no key-schema migration — but track a properly-scoped key (e.g. hashing the real identity into the key) as the real fix, not the guard. **When you do ship the properly-scoped key, keep the identity guard on top of it rather than deleting it as now-redundant** (a truncated hash can collide, and a lockfile is a hand-editable plain file), and treat the key widening as a caller-chain change — see row 42. Also check the *migration*: an existing on-disk entry under the old coarse key holds the only pin a real project has, so the new scheme must read the legacy key as a fallback (subject to the identity guard) and rewrite it under the new key, never silently ignore it — ignoring it is a cache flush disguised as a key change, and it defeats the lockfile at exactly the moment it is supposed to hold. |
| 39 | An environmental `t.Skip` guard must cover every operation depending on that precondition, and the skip-vs-fail predicate must be tested in both directions | For any test that skips on an environmental precondition ("offline", "no Docker daemon", "no Bun installed", "not in a git repo"): enumerate every call in the test body that touches the guarded resource and confirm each one is covered by the guard. Proving reachability on a cheap probe (a manifest HEAD, a version check, a socket dial) and then letting a later expensive operation fail hard is the same shape as checking a permission at open time and assuming it holds for every subsequent write — and it is worse in CI, where the misattributed failure blocks merges while naming the wrong cause. Separately: any predicate deciding *skip vs. fail* is load-bearing safety logic, because the two errors are asymmetric — too narrow merely restores a flake, while too broad silently converts a correctness gate into a fail-open that skips the very condition it exists to catch. Unit-test such a predicate with both a genuinely environmental error and a genuinely substantive one (including both wrapped behind the same `%w` message prefix), and assert the substantive one still fails. |
| 40 | A bulk rename/path sweep must classify each occurrence by role — reference vs. argument — and the sweep's own diff must be read before staging | For any mechanical sweep over a token naming a file, symbol, route, or flag (a path migration, a rename, a deprecation): a *reference* (prose, comment, doc link) is safe to rewrite in bulk, but an *argument or literal that constructs the thing* — an output path passed to a write/create/open call, an embedded template, a struct tag, a wire-format key, a string that reaches an end user at runtime — is often still correct as written, and rewriting it changes behavior rather than documentation. Grep the candidate set for the token inside a string literal handed to a filesystem, network, or template call and hand-review each hit individually. Then read the sweep's complete diff before staging: it can compile and pass every test while being wrong precisely where tests do not look — a generated-output path caught only by a freshness guard, or a runtime console message no test asserts and no human reads during CI. Corollary for moves specifically: relocating a file changes the depth of every relative link *inside* it, and if nothing regenerates that file, only a link walker will ever notice.
| 41 | One flag or input feeding several consumers must be read once, and `if err == nil { assign }` around a security control's input is a fail-open in the shape of a guard | For any flag/env var/file that more than one consumer needs: check whether the diff (or the surrounding function) reads it more than once. Duplicated reads of one input drift in their error handling, and the drift lands in whichever copy nobody is comparing against — read it once and share the value. Then, for every fallible read that feeds a verification control, look specifically for the `if data, err := read(...); err == nil { field = data }` shape (and its `if err != nil { log.Warn; }`-without-return sibling): when the un-assigned field's zero value means "use the built-in default", swallowing the error silently verifies against the default the operator explicitly set the flag to *override*. That is the exact substitution the flag exists to prevent, and it emits nothing — no warning, no log line, no non-zero exit. Fail closed and name the flag. Before concluding the codebase agrees with itself, grep for every consumer of the same flag: the correct shape often already exists in a third place with a comment explaining why, and it is worth more than reasoning from scratch. Origin: `--sigstore-trusted-root` fed `req.BaseImage.TrustedRootPath` (fail-closed) and `req.CacheVerify.TrustedRootJSON` (silent swallow, degraded to the embedded public-good root) from two separate `os.ReadFile` calls forty lines apart in one function, while `pokkum verify` had it right all along — see `Lessons.md` 2026-08-19. |
| 42 | Widening a key with a new component breaks every site that *reconstructs* it, and none of them fail to compile | When a cache/lock/map key gains a component (row 38's "real fix"), the compiler catches nothing: a helper that takes `(preset)` and looks up a `(preset, ref)`-keyed store returns "not found", not a type error, so a write silently becomes a no-op and a read silently becomes a miss. Grep every reader *and* writer of the keyed store, not just the site being changed, and give any that lacks the new component the information it needs (widen its signature) rather than letting it approximate the key. Separately, grep for every place that parses a key *back* into its components — a preset/enum parse, a prefix strip, a regex, a `switch` on the key string — because a widened key stops parsing and those sites usually `continue`/skip on failure, dropping whole categories out of user-visible output with no error. Put the key's naming vocabulary (prefix constant, key→component helper) in one package and pin the cross-package relationship with a test, since a rename in either package compiles cleanly. Origin: per-reference `pokkum.lock` slots for custom `--base` silently unhooked `RecordScanResult` (preset-only signature → recorded nothing) and `pokkum base check` (parsed each slot name as a preset → skipped every `custom:<hash>` slot) — see `Lessons.md` 2026-08-19. |
| 43 | A build-file directive is not a control until something fails when it is violated | For any line in a build/config file that appears to enforce a project decision — a `replace`, `exclude`, override, resolution pin, or a path-scoped lint disable — first prove it binds to anything at all: resolve the target (`go list -m <path>` and friends), or delete the line and diff the resolved dependency graph and lockfile. **A deletion that changes nothing is the tell that the directive was doing nothing**, and a directive matching nothing is worse than its absence, because it reads as a guarantee and suppresses the question. Note specifically that Go's `replace` takes *module* paths, so a `replace` naming a package path inside a module is always inert. Once you know the real mechanism, move the guarantee into a test that fails on violation (an import ban, an architecture assertion) rather than leaving it to convention — the end state can be correct today purely because nobody happened to write the offending line yet. Related corollary for module-granularity vulnerability alerts: "we replaced that dependency" and "no first-party code imports that package" are different claims needing different evidence, and only the second is mechanically checkable. |
| 44 | Generated input must be tested against the validator its consumer uses, and if the codebase has that validator the generator must run it | For any code that *generates* input consumed elsewhere in the same system — a scaffolded config (`pokkum init`), a default manifest, a template, an emitted flag, a test fixture — add a test asserting the generated artifact is accepted by the exact parser/validator the consuming path applies, across **every** combination the generator can emit, not just its defaults. Testing the generator's fields and the parser's rejections separately leaves the seam untested, and the seam is precisely where a value satisfying neither contract survives (a generated `sbom.attach: attestation`, plausible-sounding and never a valid mode, made `pokkum init` produce a config `pokkum build` refused). Prefer emitting from the shared constant rather than a string literal, which turns the whole class into a compile error. **If the system already contains a validator for that artifact, the generator must invoke it** — one that merely coexists with the output it governs is a control the codebase only appears to have (row 43). **This covers advice, not only data**: a command that prints "you can now run X" is generating input for the user to feed back in, so X must be a command that works given what was just written — `pokkum init` closed with a constant "run `pokkum build`" while offering a registry-less setup that makes exactly that command refuse to start. Test it by reading the recommendation out of the tool's own machine-readable output and running it, so the test follows the advice instead of hardcoding it. Two verification traps: an interactive path may not execute at all under test (a TTY check silently skipping prompts when input is piped), so choose an expected value that *differs from the default*, or a pass proves only that the default was measured; and phrase the generated-artifact test to name the offending field and value, since a generic "config is invalid" failure sends the next reader to the validator rather than to the generator that produced it. |
| 45 | A guard must be shown to fail without the fix, and if it cannot, suspect the observable before the interleaving | Row 30 already requires reverting a fix to prove its test fails. This row is the diagnostic for when that check comes back green: **do not add retries, sleeps or more goroutines first — ask what value the buggy code and the fixed code each produce, and whether your assertion can tell them apart.** A fix whose effect is invisible to the chosen observable yields a test that passes either way: a timestamp the buggy code pins to a constant (so a rewrite sets the mtime it already had), a size that does not change, a swallowed error, a log line emitted on both paths. Switch to an observable that differs — a content canary written into the artifact and checked for survival, inode identity, a call counter, a recorded argument — rather than trying harder to catch the symptom. For a race mediated by the **filesystem** rather than memory, add: `-race` is silent on it, and the vulnerable window can be one syscall wide, so a reproduction may be impossible on demand; in that case test the invariant the design actually maintains ("writes complete before any reader starts") instead of the absence of the interleaving, and say explicitly in the test file which guarantees are and are not provided — otherwise the next reader re-adds the test that asserted a guarantee you deliberately chose not to make. |

## Origin

Rows 1 and 4 were added after a real goroutine-leak bug in `internal/adapters/k8s/resolver.go`'s monorepo affected-detection fan-out loop (`mem:conventions`'s Monorepo Affected-Detection entry): an `AffectedDetector` error on a non-first project path returned before `g.Wait()`, orphaning already-dispatched build goroutines. The only existing test for that error path used a single-project manifest, so it passed without ever exercising the leak — the bug survived a prior instruction that told agents, in prose, to "scan for goroutine leaks," because nothing forced a concrete check against the specific lines where it mattered. Row 3 generalizes the same lesson from an independently-found multi-document YAML annotation cross-contamination bug in the same code area. **This exact bug recurred in this exact function on 2026-08-18** (`internal/adapters/k8s/resolver.go`'s `Resolve`): an `AffectedDetector` error on a non-first path again returned before `g.Wait()`, orphaning already-dispatched build goroutines. Rows 1 and 4 existed, named this function, and did not prevent the regression — the original fix had been guard-shaped ("return only after Wait"), which is a convention a later edit can violate without anything forcing a re-check. Fixed this time by restructuring into two passes (validate/skip-decide everything first, with zero goroutines in flight; dispatch-and-wait second) so the leak is unreachable by construction rather than avoided by discipline — rows 1 and 4 above are revised to say this explicitly. See `Lessons.md`'s 2026-08-18 "k8s affected-detection... recurrence" entry.

Row 39 was added after `TestTripwire_LiveDistroBaseImages` skipped on `remote.Image` failing (a kilobyte manifest fetch) but hard-failed on the multi-megabyte layer pull one line later, so a mid-stream `connection reset by peer` was reported as an upstream image-format change. Distinct from row 9: row 9 is about a *production* path re-checking a mutable precondition at point of use; row 39 is about a *test's* environmental skip guard covering every operation that depends on the precondition, and about the skip-vs-fail predicate itself being safety logic that needs two-directional tests.

Row 9 was added after a `supervisor/cmd/pokkum-static` SPA-fallback bug where the fallback file was validated as a regular file at construction but the serve path never re-checked it at request time, so a deletion surfaced a 500 instead of the documented 404-miss. Same Preventive rule family as Row 2 but distinct: Row 2 is about *resource cleanup on every branch*; Row 9 is about *validating a mutable runtime precondition at the point of use, not just at construction*.

Row 8 was added after a `Lessons.md` entry ("The 4-step verification suite does not run golangci-lint") documented a CI-breaking `errcheck` finding that survived every "green" report from the then-current verification protocol — `make verify` ran fmt/vet/tests/build but never lint, so a change could pass every step described in `mem:task_completion` and still fail CI on the first push. `make verify` now runs `golangci-lint run ./...` as Step 2/5 (see the Makefile), closing the gap the row exists to catch if that step is ever skipped or bypassed. Row 7 was added directly (not from a `Lessons.md` incident) per an explicit user instruction: never downgrade the Go toolchain version.

Row 10 was added after a `Lessons.md` entry (".pokkum.yaml config validation had three silent-failure gaps") found that `pokkum config validate` checked only the top-level `ProjectConfig` fields — a profile with an invalid `strategy`/`base`/`sbom`/`docker.repo` passed silently because nothing forced the same checks to run per-profile when profile support was added on top of an already-shipped top-level validator. `runConfigValidate` now extracts a shared `validateConfigFields` helper called once for the base config and once per profile; row 10 exists so the next field added to either struct doesn't quietly reintroduce the same asymmetry.

Row 11 was added after a `Lessons.md` entry ("`assets.generated.ts` normalization ran for `StrategyLayered`") found that `compiler.go`'s non-static `else` branch ran a `@jesterkit/exe-sveltekit`-only normalization step for `StrategyLayered` too, because the branch was written as "not static" instead of "is exe" back when only two strategies existed and never revisited when `StrategyLayered` was added — a real `--strategy=layered` build (the default) failed on every run against its own documented adapter. The bug shipped and stayed hidden because the same day's own golden-master test's "layered" fixture fabricated the file the buggy code expected, satisfying the bug instead of catching it (see row 12). Row 11 caught the exact same failure mode again 2026-08-18, in `bunexec.Compiler.Prepare`'s PR-5 telemetry-wiring gate: the initial implementation used `if req.Telemetry.Enabled && !req.Strategy.ApplyStatic()` — a negative check that silently included `StrategyLayered` (Pokkum's *default* strategy), for which the generated `.pokkum/telemetry-entry.ts` wrapper is never read by anything downstream (only `StrategyExe` calls `Compile()`, the sole reader of `CompileRequest.EntrypointPath`). Caught by mechanically running this exact row against the diff during self-review, before any test or user hit it; fixed by switching to a positive `switch req.Strategy { case ports.StrategyExe: ...; case ports.StrategyLayered: log.Warn(...) }`. See `Lessons.md`'s 2026-08-18 entry (bug 4) and `mem:state`'s Telemetry entry. Row 11 exists so the next new strategy, or the next branch added to this same dispatch code, gets checked against "is this arm correct for every strategy that reaches it," not just "does it compile and pass the existing tests."

Row 12 was added after the same incident's follow-up: `prerendered_patch.go`'s regression tests (added the same day, sourced from real `npm pack`'d adapter-node packages) still failed to catch that `patchPrerenderedHandler` cannot patch a *real, freshly-built* project's `handler.js` — because the checked-in fixture was adapter-node's pre-bundling source template, and the actual file the patcher reads is Vite/Rollup's bundled output, which restructures the code enough that none of the known patterns survive. "Sourced from a real npm package" felt like sufficient rigor and wasn't — it was still one pipeline stage removed from what the code under test actually consumes. That gap was fixed 2026-08-17 (see `Lessons.md`) not by adding new patterns but by discovering the matching logic itself was fine — the real bundled `handler.js` is a re-export barrel pointing at a content-hashed chunk file that still contains an exact, unchanged match of one of the existing 8 patterns; the fix was following the re-export, not rewriting the matcher. Row 12's wording was extended 2026-08-17 after a distinct but closely related incident the same day: a sub-agent-authored fixture (`compiler_strategy_test.go`'s `svCreateSvelteConfigFmt`) carried a doc comment claiming it was "captured verbatim from a real `bunx sv create` project" — untrue, caught only because adversarial review actually re-ran the claimed command. The fixture content itself was fine; the unverified provenance *claim* was the bug. See `Lessons.md`'s corresponding entry. Row 12's wording was extended again 2026-08-17 after a third, distinct incident in the same family: `internal/adapters/registry/sbom.go`'s explicit `--sbom-attach=referrer` mode silently landed SBOMs on go-containerregistry's own incompatible fallback tag scheme on any registry lacking OCI 1.1 referrers support (invisible to Pokkum's own `.sbom`-suffixed tag convention and to `cosign download sbom`) — and the one existing test for the referrer-mode success path, `TestAttachSBOM_RoundTripReferrer`, had been silently validating this exact buggy fallback as correct the whole time, because the in-process test registry it used does not enable OCI 1.1 referrers support by default. Unlike the two prior extensions (fixture *content* provenance), this was a fixture *capability* mismatch — the test double's configuration, not its data, was what didn't match the real thing being tested. Fixed by adding a capability-enabled test registry helper (`newTestRegistryWithReferrers`) and repointing the test at it. See `Lessons.md`'s 2026-08-17 `--sbom-attach=referrer` entry.

Row 13 was added after a same-day (2026-08-17) incident in the same area: a well-tested fix to `bunexec.Compiler.Prepare`'s adapter detection (`checkEffectiveAdapter`) still left every real `sv create` project unbuildable, because `Preflight` — a separate, earlier check in the real `core.Build` call chain that the unit/`Prepare`-level tests never exercised — independently and unconditionally required `svelte.config.js` to exist. Only an end-to-end test that called `core.Build` (not `Prepare` directly) against a real, `svelte.config.js`-less project surfaced it. Row 13 exists so a fix that's thoroughly tested at the function level doesn't get declared complete without checking whether anything else in the same real call chain makes an independent, untested assumption about the same input.

Row 14 was added after the root incident behind rows 12/13/this file's whole 2026-08-17 cluster: v0.2's "Zero-Config Auto-Injection" feature shipped with a virtual-config-writing code path that nothing ever read, undetected for two versions, because the one real-`bun` regression fixture (`sveltekit-basic`) has its adapter already hand-configured correctly — every test asserted things that would be equally true whether or not the injection feature did anything at all. Row 14 exists so the next "auto-*" feature's tests include a fixture that starts broken, not only fixtures that were already fine.

Row 35 was added 2026-08-19 after a test-hygiene pass found five real-build tests across two packages (`tests/integration`'s `TestFixtureDrivenE2E_Static`/`_SPAFallback`/`TestFixtureDrivenE2E_AllStrategies`/`TestRealBuildIsReproducibleAcrossRuns`/`TestRealBuild_StrategyLayered_PrerenderedRoute`, and `internal/adapters/bunexec`'s `TestLiveFixture_PreflightAndCompile`) all built directly against a checked-out `testdata/fixtures/*` directory instead of an isolated copy, despite `tests/integration/runtime_smoke_test.go` having already established the correct copy-and-symlink pattern for exactly this reason. Three distinct incidents had already surfaced pieces of this before it was generalized: `TestLiveFixture_PreflightAndCompile` failing once in a full-suite run and passing in isolation afterward (mechanism plausible, never confirmed as root cause); a static-strategy mock colliding with its own prior run's leftover output, patched ad hoc with a targeted `os.RemoveAll` instead of removing the shared directory itself; and 27 generated `.pokkum/handler-*.js` files having been committed from exactly this kind of in-place mutation. See `Lessons.md`'s 2026-08-19 "shared fixture mutation" entry.

Row 15 was added after a 2026-08-16 incident (`Lessons.md`'s stub-launcher determinism entry): `bunruntime.compileStub` compiled a constant-content entry file from inside a fresh `os.MkdirTemp` directory on every call and passed its absolute path to `bun build --compile`, making the compiled `--stub-launcher` binary non-deterministic — a direct violation of the bit-for-bit OCI reproducibility invariant, undetected because no existing test invoked the real `bun` compiler. A prior handoff document had already isolated *that* the binary was non-deterministic but not *why*, and shipped a plausible-but-wrong hypothesis (ELF `.note.gnu.build-id` linker randomness) as the leading fix candidate, based on a repro that varied two things at once (`--outfile` name *and* implicitly the build). Isolating one variable at a time (fixed outfile, varying entry path vs. fixed entry path, varying outfile) found the real cause in minutes and led to a far simpler fix (relative entry path + `cmd.Dir`) than the originally-proposed ELF binary patching. Row 15 exists so a "confirmed bug, plausible root cause" handoff still gets its root-cause hypothesis verified before a fix is designed around it — a wrong-but-plausible diagnosis can send an otherwise-careful fix down a much more complex path than the bug actually needs.

Row 16 was added after a 2026-08-17 documentation-audit incident: `pokkum explain`/`why`/`diff` shipped as three fully-wired cobra commands, marked `[x]` in the old `Roadmap.md`'s v0.4 milestone and `Done` in `AdditionalFeatures.md`'s matrix (both now `docs/archive/`), while `cmd/pokkum/explain.go` returned hardcoded literals (`Digest: "sha256:base..."`, a hardcoded `layer_index: 3`, a hardcoded `modified` list) for every input — no `remote.`, `Fetch`, or tar reader anywhere in the file. `explain_test.go` passed throughout, because it asserted the response's JSON shape, which the hardcoded data satisfies regardless of whether any real image was ever read. Three further matrix rows (supervisor `/metrics`, app-side trace-context logging, Helm/Kustomize GitOps export) were separately found marked Done while only one of their two described halves was built. The gap was found not by code review of Pokkum's own diffs but by verifying an external reviewer's claims against the code instead of against the project's own docs — the second time this project's own audit process (see `docs/archive/fixes-to-v1.md`) has caught this exact drift class, making it a recurring failure mode rather than a one-off. See `Lessons.md`'s 2026-08-17 entry and `docs/archive/Roadmap-v1-Archive.md`'s Pre-Publication Gate (item PB-1); PB-1 through PB-5 and PR-1 through PR-9 exist only there, never in the generated `docs/Roadmap.md`.

Row 17's discipline caught two further real bugs 2026-08-18, in the same function, on first real execution: `sveltekitutils.GenerateInstrumentationServer` (PR-5's OTel SDK bootstrap generator) imported two OpenTelemetry symbol names that don't exist (`TraceIDRatioBasedSampler`/`ALWAYS_OFF`; real names `TraceIdRatioBasedSampler`/`AlwaysOffSampler`) — invisible to the existing unit test, which only asserted the generated *text* contained a matching string, true or not. The same real-compile-and-run pass also discovered `@opentelemetry/auto-instrumentations-node`'s require/import-hook-based patching does not take effect under Bun's runtime at all (a real, current ecosystem gap, not a Pokkum bug) and that combining an OTLP metrics exporter with `NodeSDK` crashes once compiled via `bun build --compile` (a real Bun bundler bug producing two non-interoperable copies of a shared module). None of this was findable by reading the generated code or unit-testing its output text — only by actually compiling it against real installed dependencies and running the result. See `Lessons.md`'s 2026-08-18 entry.

Row 17 was added after the most severe bug found in this project so far: every `--strategy=layered` (default) image built by this codebase since its very first commit was missing its own entrypoint (`/app/server/index.js`) — `internal/core/pipeline.go` packaged only the SvelteKit build output's `server/` subdirectory, but a real `@sveltejs/adapter-node` build emits `index.js` as a sibling of `server/`, not inside it. Every layer/packaging test in the repo (golden digests, determinism, layer-content assertions) checks structure and bytes, never whether the packaged result actually runs — and the one test that modeled the layered strategy's directory shape at all (`tests/integration/e2e_test.go`'s `mockCompiler.Prepare`) encoded the exact same wrong assumption the production code had, so it could never have caught this. Found only by manually building a real fixture, extracting the actual packaged layer from the resulting OCI tarball, and running `bun index.js` against it directly — which crashed, twice, surfacing first the missing entrypoint and then (after an incomplete first fix) a second, deeper issue where flattening `server/` into `/app/server` broke `../../`-style relative imports chunk files use to reach siblings like `shims.js`. See `Lessons.md`'s corresponding entry for the full fix (`pruneutils.ExcludeDirs`, packaging the whole build output instead of flattening a subdirectory).

Row 18 was added after a 2026-08-17 incident found by a dedicated adversarial security review (Opus, prompted specifically to look for bypasses) rather than any test: PR-2's first cut of real kernel-enforced `--hermetic` network isolation (a Linux network namespace) was wired into `bunexec.Compiler.Prepare` (`bun run build`) only, leaving `Compiler.Compile` (`bun build --compile`, the package's own documented "stage two") completely unsandboxed — a full bypass, since Bun's bundler executes third-party build-time code (`bunfig.toml` preload plugins, `with { type: "macro" }` imports) at that stage too, on files a compromised stage-one dependency could have influenced. The fixed stage's own tests all passed throughout, because they only ever exercised the one spawn site that had been fixed. See `Lessons.md`'s corresponding entry for the four smaller findings (misdiagnosed start-vs-build failures, a `SysProcAttr`-clobbering assignment, SLSA provenance never actually recording hermetic status, and a bunruntime download path unaware of hermetic mode) from the same review.

Row 19 was added after a 2026-08-18 incident building `--hermetic-mount-isolation` (the `docker.sock` half of `--hermetic`'s residual mount-namespace gap, `internal/adapters/bunexec/hermetic_reexec_linux.go`): the first version of the empirical test overrode the package-level `hermeticMaskPaths` var in the *test process* to point at a disposable test socket, expecting the real masking mechanism to honor it — it didn't, because the actual masking runs inside a **separately-compiled real `pokkum` binary** (built specifically so the test exercises the genuine `__hermetic-reexec` subcommand, since `os.Executable()` inside a `go test` binary resolves to the test binary itself). The bug was caught immediately by actually running the test in a real Linux container (it failed with the socket still reachable) rather than by inspection — but the row exists so the next reexec-adjacent or subcommand-adjacent test catches this class of mistake before running it once, not after. Fixed by adding an explicit, narrow, test-only environment-variable channel instead (same precedent as `bunruntime.Resolver.ReleaseKeyArmored`). See `Lessons.md`'s 2026-08-18 entry.

Row 20 was added after a 2026-08-18 incident building `--asset-overlay`'s Tier 1.1 rolling-deploy feature: `internal/adapters/assetoverlay/assetoverlay.go`'s `extractImmutableAssets` stripped the wrong prefix (`immutableAssetPrefix`, `"app/client/_app/immutable/"`) when writing extracted content into its scratch directory, instead of the client root alone (`clientRootPrefix`, `"app/client/"`) — silently dropping the `_app/immutable/` segment, so repackaging under `AppClientDirPrefix` again produced files at `/app/client/chunks/...` instead of the real `/app/client/_app/immutable/chunks/...` a browser actually requests. The build succeeded, attested correctly, and every unit test passed, because those tests only asserted content landed *somewhere* under the scratch directory with the right bytes, never the exact relative path a real repackage-and-serve round-trip requires. Found only by the plan's own designated row-17-discipline empirical test (`tests/integration/asset_overlay_e2e_test.go`'s `TestRealBuild_AssetOverlay_TwoGenerations`, task #86) — two real, sequential `core.Build`+push runs against a real in-memory registry, inspecting the actually-pushed image's real tar entry names — which is exactly the scenario this checklist's Row 17 exists to force in the first place, just for a static-asset path rather than an executable entrypoint. See `Lessons.md`'s 2026-08-18 asset-overlay entry.

Rows 21–23 were added after a 2026-08-18 adversarial (Opus) review of `--asset-overlay`, run as this codebase's own standing final-review step (CLAUDE.md §6.3) immediately after the feature's full test suite — including a real two-generation empirical test that had *already* caught one severe bug the same day — passed green. The review found four further real, independently-confirmed defects none of that testing caught: (row 21) a second attestation-digest bug, distinct from the earlier same-day one — two OCI layers (the asset overlay and the current build's own client layer) both target `/app/client`, so a path present in both got double-counted in the aggregate digest, since `attestutils.RootDigest` has no dedup of its own; the feature's own test's independent oracle mirrored the same non-dedup bug, so it agreed with the code instead of catching it. (row 22) a real path-traversal vulnerability in `extractImmutableAssets` — a raw, uncleaned tar entry name could satisfy a prefix filter while still containing an unresolved `../` that only got resolved later by `filepath.Join`, escaping the destination directory; the content this filter gates is registry-pulled (auto-discovered chain, or an arbitrary caller-supplied `--asset-overlay-from` ref), not locally-trusted. (row 23) `--asset-overlay-from`'s cross-repository refs always 404'd and hard-failed the build, because `ResolveDigest` returned only a bare digest, silently losing which repository it actually resolved in, while the caller then pulled every resolved digest from its own default repo regardless — directly contradicting the flag's own documented "arbitrary external images" contract. All three were confirmed empirically (a real mismatched digest against a real pushed image; a real file written outside a real temp directory; a real cross-repo build reproducing the exact `NAME_UNKNOWN` failure) before being fixed, and each fix's test was verified to genuinely fail when the fix was temporarily reverted. A fourth, related pair of findings (unbounded `io.ReadAll` on registry content; a malformed predecessor annotation hard-failing the whole chain-walk instead of truncating like the adjacent not-found case) extended existing discipline (row 15's family, and the "fail closed only where earned" principle) rather than needing new rows. See `Lessons.md`'s 2026-08-18 "Adversarial review of `--asset-overlay`" entry for full details, including why the feature's own extensive real-world testing — genuinely rigorous by this project's usual standard — still didn't surface any of these four: ordinary test-writing naturally builds fixtures that exercise the *intended* input shape, not overlapping, malicious, or cross-scope input, which is exactly what a dedicated adversarial pass exists to force.

Rows 16b, 24, 25, and 26 were added 2026-08-18 after a documentation-and-memory-sync pass following a batch of seven fixes on `feat/monorepo-affected2` (image signing wired end-to-end for the first time; keyless verification's self-referential identity bug; secretguard's `bufio.Scanner`/one-match-per-line bugs; two smaller validation-logic bugs in the CVE gate and the Bun SHASUMS parser). The signing incident (row 16b) is a direct, exact recurrence of row 16's own failure mode — `CosignSigner`/`DSSESigner` were required non-nil by validation and never once invoked by the pipeline — which row 16 itself, already in this checklist, would have caught in one grep command had it been run against this exact code; 16b exists to make that specific sub-case unmissable rather than trusting that row 16's broader wording gets applied to every dependency-injection shape. Row 24 generalizes the keyless-identity incident beyond Sigstore specifically: any verification path that derives its expected value from the artifact under test is tautological, and the naive-seeming fix (compare the "real" field instead of the wrong one, but still read it off the same artifact) can be strictly worse than the original bug. Row 25 generalizes the signing incident's actual fix (fetch-back-and-reverify) into a standing design principle for any security control, not just signing. Row 26 generalizes secretguard's bug into a general Go text-scanning trap (`bufio.Scanner`'s 64KB default) that will recur in any future line-oriented scanner over build output. See `Lessons.md`'s five 2026-08-18 entries for full incident detail on each.

Rows 27–31 were added 2026-08-18 after a batch of fixes on `feat/monorepo-affected2` that removed a shared placeholder trust anchor (row 30's origin: `internal/adapters/provenance/resolver.go`'s static-key path was a literal fail-open, `SignatureValid: false` with a `nil` error, found only by applying the same "does this fail closed with no key configured?" proof to a third site after two siblings had already been confirmed fixed) and pinned previously `SOURCE_DATE_EPOCH`-derived immutable-binary layer timestamps (rows 27/28/31's origin: fixing Roadmap item 3f — originally framed as "write a test asserting the Bun layer diffID is stable" — found the diffID was not stable, and writing the fix surfaced two more distinct test-quality bugs in the same area: `tests/integration/e2e_test.go` had an assertion that pinned the *buggy* timestamp behavior as correct, which would have actively resisted the fix, and the new stability regression test needed per-run cache isolation or a shared, now-more-permissive content-only cache key would have made two intentionally-varied builds collapse into one, passing regardless of whether the fix worked). Row 29's origin: the same day's runtime-boot smoke test (`tests/integration/runtime_smoke_test.go`, row 17's test) leaked a Docker container on its own failure path, because `docker run -d --name X` registers the container before starting it, and cleanup had been registered only after a confirmed-successful run. See `Lessons.md`'s five 2026-08-18 entries for full incident detail (git `--since` injection and a `comparator` bounds-check gap were fixed the same day but matched existing rows well enough not to need new ones — the injection fix is row-less process discipline ["reuse a validated value, don't recompute/discard it"] rather than a checklist-shaped code-review check, and the bounds-check gap is already covered by row 2's "every branch" discipline applied to validity checks instead of cleanup).

Rows 32–34 were added 2026-08-19 after a documentation-and-memory-sync pass following a batch of overnight fixes on `feat/monorepo-affected2`: a real `--strategy=static` fixture immediately found three production bugs (a `pokkum-static` bind-address bug rows 32/33/34 don't individually cover but which is its own Lessons.md entry; `Preflight` repeating row 13's exact failure mode; a synthetic fixture repeating row 12's), the composition-root refactor that emptied the adapter-adapter allowlist exposed two more `false, nil`-shaped fail-opens in `provenance` (row 32's origin — the third occurrence of this shape in roughly a day, which is what makes it a named row rather than a one-off note), and the Sigstore trust-root refresh found the embedded snapshot was not merely stale but already rejecting valid signatures, while the embedded PID-1 binaries turned out to be gitignored and built by no CI or release pipeline at all (row 33's origin) and the trust-root fix's own design is row 34's origin — a capture date, a failing-not-warning age test, and a diagnosis that separates "our anchor doesn't cover this" from "this signature is bad." See `Lessons.md`'s six 2026-08-19 entries for full incident detail on each.

Row 31 was extended, and rows 36–38 added, 2026-08-19 after the final batch of overnight fixes on `feat/monorepo-affected2`. Row 31's extension has two origins: Go's `-buildvcs` default stamped `vcs.revision`/`vcs.time`/`vcs.modified` into the embedded PID-1 binaries, churning their content on every commit even with byte-identical source — the second instance, after `SOURCE_DATE_EPOCH`, of build metadata leaking into these same content-invariant layers; and the blob-freshness guard built to catch exactly this kind of drift initially proved nothing, because its "build twice and compare" reproducibility check ran both builds at one commit and one dirty-state, never varying the dimension (commit-to-commit) that actually matters. Rather than add a sibling row for the second half, row 31 itself was broadened, since both halves are the same underlying class: a build-time value leaking into (or a check failing to vary across) an artifact whose bytes are supposed to be content-determined, not process-determined. Row 36 was added after `cosign verify-attestation` rejected every Pokkum attestation over a missing `dev.cosignproject.cosign/signature` annotation — the assumption that the annotation was signature-only and could be omitted for attestations was encoded in a code comment and never checked against cosign v3's own source, which (via `Base64Signature()`) errors on the annotation *key* being absent regardless of its value, and always writes it as an empty string for attestations. Row 37 was added after the same overnight session found four real issues (three product-side, one in the guide itself) purely by running `paranoid-testing-guide.md`'s commands against a real build rather than reading them — including the guide's own `--output=json`/`jq` extraction, reused in eight places, which could never have worked because `build`'s stdout is deliberately a bare `repo@sha256:...` ref. Row 38 was added after every custom `--base` reference was found to share one `"custom"` `pokkum.lock` slot: resolving custom base B after custom base A had been locked trusted A's entry and silently returned A's image content for a B request — not a cache miss, a wrong answer — reachable only once the CLI gained a way to supply a custom reference at all (the same commit that fixed the CLI gap found this latent lock-collision bug while implementing it). See `Lessons.md`'s 2026-08-19 entries for full incident detail on each.

## Maintenance rule

This table is edited in place, not appended to. Every time a `Lessons.md` entry is logged: check whether its category matches an existing row here.
- If it matches but the row didn't catch it, revise the row's wording so this exact failure mode is unambiguously covered next time.
- If it's a genuinely new category, add a row.

A `Lessons.md` entry with no corresponding update here is a bug likely to recur — this checklist, not the log, is what actually gets run during the next self-review.
