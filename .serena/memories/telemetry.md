# OpenTelemetry & Unified Metrics Architecture

> **⚠️ CURRENTLY NON-FUNCTIONAL, found 2026-08-17 (see `Lessons.md`'s PR-5 entry and `Roadmap.md`'s PR-5 row — do not treat the subsystems below as wired just because they exist and pass their own unit tests).** Every piece described below is individually implemented and individually unit-tested, but nothing in the real build pipeline calls any of it: `internal/core/pipeline.go`'s `ports.PrepareRequest{}` construction never sets `Telemetry: req.Telemetry`, so `internal/adapters/bunexec/compiler.go`'s `Compiler.Prepare` never reads `req.Telemetry` and never calls `sveltekitutils.PrepareVirtualInstrumentation` — confirmed by grepping the whole repo for every caller of `PrepareVirtualInstrumentation`/`GenerateInstrumentationServer`/`opts.EnableTelemetry` and finding none outside their own test files. The `TransformConfig`/`PrepareVirtualConfig` half (svelte.config.js `kit.experimental` flags) is separately dead: `compiler.go`'s own code comment already documents that its `.pokkum/svelte.config.js` output is never read by either build path (real `bun run build` reads the real, unmodified `svelte.config.js`; the Option B wrapper only swaps `vite.config.ts`, and SvelteKit's Vite plugin independently loads `svelte.config.js` from the project root regardless of which `vite.config.ts` was passed to `vite build --config`). `--telemetry`/`.pokkum.yaml`'s `otel.*` settings parse and validate correctly but have zero effect on a built image today. Before touching this subsystem again, re-verify this note against the current code (grep the same call sites) rather than trusting it blindly — but don't assume it's been fixed just because time has passed, either.
>
> A real fix needs the Option B virtual-config wrapper (`bunexec.Compiler.Prepare`'s `runViteWrapper` branch) to engage whenever `req.Telemetry.Enabled`, independent of whether adapter injection is needed — a change to the build-dispatch trigger condition for every build, not a narrow patch, and one that needs genuine end-to-end verification (a real instrumented `bun run build` that actually starts and exports spans) before it can be trusted, matching `mem:self_review_checklist` rows 11 and 17.

Pokkum unifies trace spans and metrics into a native, zero-config OpenTelemetry pipeline built on SvelteKit 2.31+ native observability (`kit.experimental.tracing.server` and `kit.experimental.instrumentation.server`) — **this describes the intended design, not current behavior; see the warning above.**

## Key Subsystems
- **Version Inspector (`internal/adapters/sveltekitutils/project.go`)**: Parses `@sveltejs/kit` version from `package.json` (`CheckTelemetrySupported`). Skips telemetry if version < 2.31.0.
- **Single-Pass Config Transformer (`internal/adapters/sveltekitutils/injector.go`)**: Updates `svelte.config.js` in a single virtual pass (`TransformConfig`), combining adapter replacement, `SOURCE_DATE_EPOCH` version pinning, and `kit.experimental` telemetry flag injection.
- **Strict Precedence & Virtual Instrumentation (`internal/adapters/sveltekitutils/telemetry.go`)**: Checks `InstrumentationExists` for `src/instrumentation.server.ts|js|mjs`. If present, user file is preserved. If missing, writes virtual `.pokkum/src/instrumentation.server.ts` configured with lazy SDK init, trace sampling (`--trace-sample-rate`), metrics-only mode (`--metrics-only`), and OTLP exporters.
- **OTEL Collector K8s Sidecar (`internal/adapters/k8s/resolver.go`)**: When `--with-otel-sidecar` is set, `pokkum resolve` and `pokkum apply` inject an OpenTelemetry Collector sidecar container spec (`4317` gRPC, `4318` HTTP, `8889` metrics) into Pod manifests.
- **CLI Options (`cmd/pokkum/build.go`)**: Exposes `--telemetry`, `--no-telemetry`, `--otel-export`, `--telemetry-env`, `--trace-sample-rate`, `--metrics-only`, `--with-otel-sidecar`.
